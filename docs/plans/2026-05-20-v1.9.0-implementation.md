# v1.9.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar operações de arquivo no Explorer (novo arquivo, nova pasta, copy/cut/paste, drag-and-drop) e fechar abas órfãs ao trocar workspace.

**Architecture:** `FileOperationsService` (novo, stateless, testável) encapsula lógica de FS. `ExplorerPanel` adiciona items no ContextMenu + InputBindings + handlers de drag-and-drop. `MainViewModel` ganha `CloseTabsOutsideWorkspace`. Dialog reutilizável `InputDialog` pra perguntar nome ao criar.

**Tech Stack:** .NET 10 WPF, xUnit 2.9.3, Markdig 1.1.1 (sem impacto).

**Spec:** [docs/specs/2026-05-20-v1.9.0-design.md](../specs/2026-05-20-v1.9.0-design.md)

---

## Fase 0 — Preparação

### Task 0.1: Branch + baseline

**Files:**
- Verify: `src/MeuMarkdown/MeuMarkdown.csproj` (estado main pós v1.8.0)

- [ ] **Step 1: Criar branch feature**

Run:
```powershell
rtk git checkout main
rtk git pull origin main
rtk git checkout -b feat/v1.9.0
```

Expected: branch criada baseada em main pós v1.8.0.

- [ ] **Step 2: Confirmar build + testes verdes**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
rtk dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --no-build --nologo -v minimal 2>&1 | rtk tail -n 3
```

Expected: build 0 erros, ~114 testes passam (baseline pós v1.8.0).

---

## Fase 1 — `FileOperationsService` (TDD)

### Task 1.1: Esqueleto + testes do `GenerateUniqueName`

**Files:**
- Create: `src/MeuMarkdown/Services/FileOperationsService.cs`
- Create: `tests/MeuMarkdown.Tests/Services/FileOperationsServiceTests.cs`

- [ ] **Step 1: Criar arquivo do serviço (esqueleto vazio)**

Conteúdo de `src/MeuMarkdown/Services/FileOperationsService.cs`:

```csharp
using System.IO;

namespace MeuMarkdown.Services;

public class FileOperationException : Exception
{
    public FileOperationException(string message) : base(message) { }
    public FileOperationException(string message, Exception inner) : base(message, inner) { }
}

public class FileOperationsService
{
    /// <summary>
    /// Gera um nome único no diretório: se "baseName.ext" não existe, retorna ele;
    /// se existe, tenta "baseName (2).ext", "baseName (3).ext", etc.
    /// Para pasta (extension null/vazio), gera "baseName", "baseName (2)", etc.
    /// </summary>
    public static string GenerateUniqueName(string directory, string baseName, string? extension)
    {
        var ext = string.IsNullOrEmpty(extension) ? string.Empty : extension;
        if (!ext.StartsWith('.') && ext.Length > 0) ext = "." + ext;

        var candidate = Path.Combine(directory, baseName + ext);
        if (!Exists(candidate)) return baseName + ext;

        for (int i = 2; i < 1000; i++)
        {
            var name = $"{baseName} ({i}){ext}";
            candidate = Path.Combine(directory, name);
            if (!Exists(candidate)) return name;
        }
        throw new FileOperationException($"Não foi possível gerar nome único para {baseName}{ext} (mais de 1000 tentativas).");
    }

    private static bool Exists(string path) => File.Exists(path) || Directory.Exists(path);
}
```

- [ ] **Step 2: Criar arquivo de testes**

Conteúdo de `tests/MeuMarkdown.Tests/Services/FileOperationsServiceTests.cs`:

```csharp
using MeuMarkdown.Services;
using Xunit;

namespace MeuMarkdown.Tests.Services;

public class FileOperationsServiceTests : IDisposable
{
    private readonly string _tmpDir;

    public FileOperationsServiceTests()
    {
        _tmpDir = Path.Combine(Path.GetTempPath(),
            "mm-fileops-" + Guid.NewGuid().ToString("N").Substring(0, 8));
        Directory.CreateDirectory(_tmpDir);
    }

    public void Dispose()
    {
        try { Directory.Delete(_tmpDir, recursive: true); } catch { }
    }

    [Fact]
    public void GenerateUniqueName_NoConflict_ReturnsBaseName()
    {
        var name = FileOperationsService.GenerateUniqueName(_tmpDir, "foo", ".md");
        Assert.Equal("foo.md", name);
    }

    [Fact]
    public void GenerateUniqueName_OneConflict_ReturnsWithSuffix2()
    {
        File.WriteAllText(Path.Combine(_tmpDir, "foo.md"), "");
        var name = FileOperationsService.GenerateUniqueName(_tmpDir, "foo", ".md");
        Assert.Equal("foo (2).md", name);
    }

    [Fact]
    public void GenerateUniqueName_TwoConflicts_ReturnsWithSuffix3()
    {
        File.WriteAllText(Path.Combine(_tmpDir, "foo.md"), "");
        File.WriteAllText(Path.Combine(_tmpDir, "foo (2).md"), "");
        var name = FileOperationsService.GenerateUniqueName(_tmpDir, "foo", ".md");
        Assert.Equal("foo (3).md", name);
    }

    [Fact]
    public void GenerateUniqueName_NoExtension_TreatsAsFolder()
    {
        var name = FileOperationsService.GenerateUniqueName(_tmpDir, "nova-pasta", null);
        Assert.Equal("nova-pasta", name);
    }

    [Fact]
    public void GenerateUniqueName_FolderConflict_ReturnsWithSuffix()
    {
        Directory.CreateDirectory(Path.Combine(_tmpDir, "nova-pasta"));
        var name = FileOperationsService.GenerateUniqueName(_tmpDir, "nova-pasta", null);
        Assert.Equal("nova-pasta (2)", name);
    }

    [Fact]
    public void GenerateUniqueName_ExtensionWithoutDot_NormalizesIt()
    {
        var name = FileOperationsService.GenerateUniqueName(_tmpDir, "foo", "md");
        Assert.Equal("foo.md", name);
    }
}
```

- [ ] **Step 3: Build + rodar testes — 6 PASS**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
rtk dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter FileOperationsServiceTests --nologo -v minimal 2>&1 | rtk tail -n 5
```

Expected: 6/6 PASS.

- [ ] **Step 4: Commit**

Run:
```powershell
rtk git add src/MeuMarkdown/Services/FileOperationsService.cs tests/MeuMarkdown.Tests/Services/FileOperationsServiceTests.cs
rtk git commit -m "feat(fileops): GenerateUniqueName com testes (TDD baseline)"
```

### Task 1.2: `CreateNewFile` + `CreateNewFolder`

**Files:**
- Modify: `src/MeuMarkdown/Services/FileOperationsService.cs`
- Modify: `tests/MeuMarkdown.Tests/Services/FileOperationsServiceTests.cs`

- [ ] **Step 1: Adicionar testes**

Anexar em `FileOperationsServiceTests.cs` antes da `Dispose`:

```csharp
[Fact]
public void CreateNewFile_NewFile_CreatesEmptyFile()
{
    var svc = new FileOperationsService();
    var path = svc.CreateNewFile(_tmpDir, "novo", ".md");

    Assert.True(File.Exists(path));
    Assert.EndsWith("novo.md", path);
    Assert.Equal(string.Empty, File.ReadAllText(path));
}

[Fact]
public void CreateNewFile_AlreadyExists_AppendsSuffix()
{
    File.WriteAllText(Path.Combine(_tmpDir, "novo.md"), "");
    var svc = new FileOperationsService();
    var path = svc.CreateNewFile(_tmpDir, "novo", ".md");

    Assert.EndsWith("novo (2).md", path);
}

[Fact]
public void CreateNewFolder_NewFolder_CreatesDirectory()
{
    var svc = new FileOperationsService();
    var path = svc.CreateNewFolder(_tmpDir, "minha-pasta");

    Assert.True(Directory.Exists(path));
    Assert.EndsWith("minha-pasta", path);
}

[Fact]
public void CreateNewFolder_AlreadyExists_AppendsSuffix()
{
    Directory.CreateDirectory(Path.Combine(_tmpDir, "minha-pasta"));
    var svc = new FileOperationsService();
    var path = svc.CreateNewFolder(_tmpDir, "minha-pasta");

    Assert.EndsWith("minha-pasta (2)", path);
}
```

- [ ] **Step 2: Rodar — 4 FAIL (métodos inexistentes)**

Run:
```powershell
rtk dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter FileOperationsServiceTests --nologo -v minimal 2>&1 | rtk tail -n 5
```

Expected: build falha por método inexistente.

- [ ] **Step 3: Implementar métodos**

Adicionar em `FileOperationsService.cs` antes da chave de fechamento da classe:

```csharp
public string CreateNewFile(string directory, string baseName, string extension)
{
    if (!Directory.Exists(directory))
        throw new FileOperationException($"Diretório não existe: {directory}");

    var name = GenerateUniqueName(directory, baseName, extension);
    var fullPath = Path.Combine(directory, name);
    try
    {
        File.WriteAllText(fullPath, string.Empty);
        return fullPath;
    }
    catch (Exception ex)
    {
        throw new FileOperationException($"Erro ao criar arquivo '{name}': {ex.Message}", ex);
    }
}

public string CreateNewFolder(string directory, string baseName)
{
    if (!Directory.Exists(directory))
        throw new FileOperationException($"Diretório não existe: {directory}");

    var name = GenerateUniqueName(directory, baseName, null);
    var fullPath = Path.Combine(directory, name);
    try
    {
        Directory.CreateDirectory(fullPath);
        return fullPath;
    }
    catch (Exception ex)
    {
        throw new FileOperationException($"Erro ao criar pasta '{name}': {ex.Message}", ex);
    }
}
```

- [ ] **Step 4: Rodar — 10/10 PASS (6 anteriores + 4 novos)**

Run:
```powershell
rtk dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter FileOperationsServiceTests --nologo -v minimal 2>&1 | rtk tail -n 3
```

- [ ] **Step 5: Commit**

Run:
```powershell
rtk git add src/MeuMarkdown/Services/FileOperationsService.cs tests/MeuMarkdown.Tests/Services/FileOperationsServiceTests.cs
rtk git commit -m "feat(fileops): CreateNewFile e CreateNewFolder"
```

### Task 1.3: `CopyFile` + `MoveFile`

**Files:**
- Modify: `src/MeuMarkdown/Services/FileOperationsService.cs`
- Modify: `tests/MeuMarkdown.Tests/Services/FileOperationsServiceTests.cs`

- [ ] **Step 1: Adicionar testes**

Anexar antes da `Dispose`:

```csharp
[Fact]
public void CopyFile_NoConflict_CopiesToDest()
{
    var src = Path.Combine(_tmpDir, "src.md");
    File.WriteAllText(src, "conteúdo");
    var destDir = Path.Combine(_tmpDir, "sub");
    Directory.CreateDirectory(destDir);

    var svc = new FileOperationsService();
    var result = svc.CopyFile(src, destDir, onConflict: null);

    Assert.NotNull(result);
    Assert.Equal(Path.Combine(destDir, "src.md"), result);
    Assert.True(File.Exists(src), "Original deve continuar existindo");
    Assert.Equal("conteúdo", File.ReadAllText(result));
}

[Fact]
public void CopyFile_SameDir_AutoIncrementsName()
{
    var src = Path.Combine(_tmpDir, "foo.md");
    File.WriteAllText(src, "x");

    var svc = new FileOperationsService();
    var result = svc.CopyFile(src, _tmpDir, onConflict: null);

    Assert.EndsWith("foo (2).md", result);
    Assert.True(File.Exists(src));
    Assert.True(File.Exists(result));
}

[Fact]
public void CopyFile_DestExistsAndCallbackReturnsFalse_ReturnsNull()
{
    var src = Path.Combine(_tmpDir, "foo.md");
    File.WriteAllText(src, "src");
    var destDir = Path.Combine(_tmpDir, "sub");
    Directory.CreateDirectory(destDir);
    File.WriteAllText(Path.Combine(destDir, "foo.md"), "dest-existing");

    var svc = new FileOperationsService();
    var result = svc.CopyFile(src, destDir, onConflict: _ => false);

    Assert.Null(result);
    Assert.Equal("dest-existing", File.ReadAllText(Path.Combine(destDir, "foo.md")));
}

[Fact]
public void CopyFile_DestExistsAndCallbackReturnsTrue_Overwrites()
{
    var src = Path.Combine(_tmpDir, "foo.md");
    File.WriteAllText(src, "new-content");
    var destDir = Path.Combine(_tmpDir, "sub");
    Directory.CreateDirectory(destDir);
    File.WriteAllText(Path.Combine(destDir, "foo.md"), "old-content");

    var svc = new FileOperationsService();
    var result = svc.CopyFile(src, destDir, onConflict: _ => true);

    Assert.NotNull(result);
    Assert.Equal("new-content", File.ReadAllText(result));
}

[Fact]
public void MoveFile_NoConflict_MovesToDest()
{
    var src = Path.Combine(_tmpDir, "src.md");
    File.WriteAllText(src, "x");
    var destDir = Path.Combine(_tmpDir, "sub");
    Directory.CreateDirectory(destDir);

    var svc = new FileOperationsService();
    var result = svc.MoveFile(src, destDir, onConflict: null);

    Assert.NotNull(result);
    Assert.False(File.Exists(src), "Original NÃO deve existir após move");
    Assert.True(File.Exists(result));
}

[Fact]
public void MoveFile_DestExistsAndCallbackReturnsTrue_Overwrites()
{
    var src = Path.Combine(_tmpDir, "foo.md");
    File.WriteAllText(src, "new");
    var destDir = Path.Combine(_tmpDir, "sub");
    Directory.CreateDirectory(destDir);
    File.WriteAllText(Path.Combine(destDir, "foo.md"), "old");

    var svc = new FileOperationsService();
    var result = svc.MoveFile(src, destDir, onConflict: _ => true);

    Assert.NotNull(result);
    Assert.False(File.Exists(src));
    Assert.Equal("new", File.ReadAllText(result));
}
```

- [ ] **Step 2: Implementar métodos**

Adicionar em `FileOperationsService.cs` antes da chave de fechamento:

```csharp
/// <summary>
/// Copia source para destDirectory. Se nome já existe no destino:
/// - Se sourceDirectory == destDirectory: gera nome único automaticamente (sem callback).
/// - Senão: invoca onConflict; true → sobrescreve; false ou null → retorna null.
/// </summary>
public string? CopyFile(string sourcePath, string destDirectory, Func<string, bool>? onConflict)
{
    if (!File.Exists(sourcePath))
        throw new FileOperationException($"Arquivo origem não existe: {sourcePath}");
    if (!Directory.Exists(destDirectory))
        throw new FileOperationException($"Diretório destino não existe: {destDirectory}");

    var sourceDir = Path.GetDirectoryName(sourcePath) ?? "";
    var fileName = Path.GetFileName(sourcePath);
    var destPath = Path.Combine(destDirectory, fileName);

    if (string.Equals(sourceDir, destDirectory, StringComparison.OrdinalIgnoreCase))
    {
        // Mesmo dir: gera nome único automaticamente
        var baseName = Path.GetFileNameWithoutExtension(fileName);
        var ext = Path.GetExtension(fileName);
        var uniqueName = GenerateUniqueName(destDirectory, baseName, ext);
        destPath = Path.Combine(destDirectory, uniqueName);
    }
    else if (File.Exists(destPath) || Directory.Exists(destPath))
    {
        if (onConflict == null || !onConflict(destPath))
            return null;
    }

    try
    {
        File.Copy(sourcePath, destPath, overwrite: true);
        return destPath;
    }
    catch (Exception ex)
    {
        throw new FileOperationException($"Erro ao copiar '{fileName}': {ex.Message}", ex);
    }
}

/// <summary>
/// Move source para destDirectory. Mesma lógica de conflito do CopyFile, exceto:
/// move no mesmo dir é no-op (retorna sourcePath sem mudança).
/// </summary>
public string? MoveFile(string sourcePath, string destDirectory, Func<string, bool>? onConflict)
{
    if (!File.Exists(sourcePath))
        throw new FileOperationException($"Arquivo origem não existe: {sourcePath}");
    if (!Directory.Exists(destDirectory))
        throw new FileOperationException($"Diretório destino não existe: {destDirectory}");

    var sourceDir = Path.GetDirectoryName(sourcePath) ?? "";
    if (string.Equals(sourceDir, destDirectory, StringComparison.OrdinalIgnoreCase))
        return sourcePath; // no-op

    var fileName = Path.GetFileName(sourcePath);
    var destPath = Path.Combine(destDirectory, fileName);

    if (File.Exists(destPath) || Directory.Exists(destPath))
    {
        if (onConflict == null || !onConflict(destPath))
            return null;
        // Deleta o existente pra File.Move conseguir
        if (File.Exists(destPath)) File.Delete(destPath);
        else Directory.Delete(destPath, recursive: true);
    }

    try
    {
        File.Move(sourcePath, destPath);
        return destPath;
    }
    catch (Exception ex)
    {
        throw new FileOperationException($"Erro ao mover '{fileName}': {ex.Message}", ex);
    }
}
```

- [ ] **Step 3: Build + rodar — 16/16 PASS (10 anteriores + 6 novos)**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
rtk dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter FileOperationsServiceTests --nologo -v minimal 2>&1 | rtk tail -n 3
```

- [ ] **Step 4: Commit**

Run:
```powershell
rtk git add src/MeuMarkdown/Services/FileOperationsService.cs tests/MeuMarkdown.Tests/Services/FileOperationsServiceTests.cs
rtk git commit -m "feat(fileops): CopyFile e MoveFile com onConflict callback"
```

---

## Fase 2 — Dialog de input (reutilizável)

### Task 2.1: Criar `InputDialog`

**Files:**
- Create: `src/MeuMarkdown/Controls/InputDialog.xaml`
- Create: `src/MeuMarkdown/Controls/InputDialog.xaml.cs`

- [ ] **Step 1: Criar XAML**

Conteúdo de `src/MeuMarkdown/Controls/InputDialog.xaml`:

```xml
<Window x:Class="MeuMarkdown.Controls.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle, RelativeSource={RelativeSource Mode=Self}}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource Brush.Bg.Primary}">
    <StackPanel Margin="20">
        <TextBlock x:Name="PromptText" Margin="0,0,0,10"
                   Foreground="{DynamicResource Brush.Fg.Primary}" />
        <TextBox x:Name="InputBox" Padding="6"
                 Background="{DynamicResource Brush.Bg.Secondary}"
                 Foreground="{DynamicResource Brush.Fg.Primary}"
                 BorderBrush="{DynamicResource Brush.Border}"
                 BorderThickness="1"
                 CaretBrush="{DynamicResource Brush.Fg.Primary}"
                 KeyDown="OnInputKeyDown" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" Width="80" Margin="0,0,8,0" IsDefault="True" Click="OnOk" />
            <Button Content="Cancelar" Width="80" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
```

- [ ] **Step 2: Criar code-behind**

Conteúdo de `src/MeuMarkdown/Controls/InputDialog.xaml.cs`:

```csharp
using System.Windows;
using System.Windows.Input;

namespace MeuMarkdown.Controls;

public partial class InputDialog : Window
{
    public string DialogTitle { get; set; } = "Entrada";
    public string Result { get; private set; } = string.Empty;

    public InputDialog(string title, string prompt, string defaultValue)
    {
        InitializeComponent();
        DialogTitle = title;
        Title = title;
        PromptText.Text = prompt;
        InputBox.Text = defaultValue;
        InputBox.SelectAll();
        InputBox.Focus();
    }

    private void OnOk(object sender, RoutedEventArgs e)
    {
        Result = InputBox.Text;
        DialogResult = true;
    }

    private void OnInputKeyDown(object sender, KeyEventArgs e)
    {
        if (e.Key == Key.Enter)
        {
            Result = InputBox.Text;
            DialogResult = true;
        }
    }

    /// <summary>
    /// Mostra o dialog modal e retorna o texto digitado, ou null se cancelado.
    /// </summary>
    public static string? Show(Window owner, string title, string prompt, string defaultValue)
    {
        var dlg = new InputDialog(title, prompt, defaultValue) { Owner = owner };
        return dlg.ShowDialog() == true ? dlg.Result : null;
    }
}
```

- [ ] **Step 3: Build**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

Expected: 0 erros.

- [ ] **Step 4: Commit**

Run:
```powershell
rtk git add src/MeuMarkdown/Controls/InputDialog.xaml src/MeuMarkdown/Controls/InputDialog.xaml.cs
rtk git commit -m "feat(ui): InputDialog reutilizavel para perguntar nomes"
```

---

## Fase 3 — Novo arquivo + Nova pasta

### Task 3.1: Wire `FileOperationsService` no `ExplorerPanel`

**Files:**
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs`

- [ ] **Step 1: Adicionar field e helper de target directory**

Em `ExplorerPanel.xaml.cs`, perto dos outros fields no início da classe:

```csharp
private readonly FileOperationsService _fileOps = new();
```

E adicionar antes do `OnRename`:

```csharp
/// <summary>
/// Retorna o diretório onde operações de criar/colar devem ocorrer:
/// - Item selecionado é pasta → essa pasta
/// - Item selecionado é arquivo → diretório do arquivo
/// - Sem seleção → root do workspace (ou null se workspace fechado)
/// </summary>
private string? GetTargetDirectory()
{
    if (FileTree.SelectedItem is FileNode node)
    {
        return node.IsDirectory ? node.FullPath : Path.GetDirectoryName(node.FullPath);
    }
    return _workspaceService?.RootPath;
}
```

- [ ] **Step 2: Build (compila? — pode faltar `using System.IO`)**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

Se faltar using, adicionar:
```csharp
using System.IO;
using MeuMarkdown.Services;
```

(Provavelmente já têm — só confirmar.)

### Task 3.2: Handler `OnNewFile`

**Files:**
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs`
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml`

- [ ] **Step 1: Adicionar handler em `ExplorerPanel.xaml.cs`**

Adicionar após `OnRefresh` ou perto dos outros handlers de menu:

```csharp
private void OnNewFile(object sender, RoutedEventArgs e)
{
    var targetDir = GetTargetDirectory();
    if (targetDir == null) return;

    var suggested = FileOperationsService.GenerateUniqueName(targetDir, "novo-arquivo", ".md");
    var owner = Window.GetWindow(this);
    if (owner == null) return;

    var name = Controls.InputDialog.Show(owner, "Novo arquivo",
        $"Nome do novo arquivo em '{Path.GetFileName(targetDir)}':", suggested);
    if (string.IsNullOrWhiteSpace(name)) return;

    if (!name.EndsWith(".md", StringComparison.OrdinalIgnoreCase) &&
        !name.EndsWith(".markdown", StringComparison.OrdinalIgnoreCase))
    {
        name += ".md";
    }

    try
    {
        var baseName = Path.GetFileNameWithoutExtension(name);
        var ext = Path.GetExtension(name);
        var newPath = _fileOps.CreateNewFile(targetDir, baseName, ext);
        _workspaceService?.Refresh();
        FileActivated?.Invoke(this, newPath);
    }
    catch (FileOperationException ex)
    {
        MessageBox.Show(ex.Message, "Erro", MessageBoxButton.OK, MessageBoxImage.Error);
    }
}
```

Se faltar using `MeuMarkdown.Controls`, adicionar (talvez já tenha):

```csharp
using MeuMarkdown.Controls;
```

Nota: `Controls.InputDialog.Show(...)` se já houver `using MeuMarkdown.Controls`, pode usar `InputDialog.Show(...)` direto.

- [ ] **Step 2: Adicionar item no ContextMenu (em ExplorerPanel.xaml)**

Em `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml`, na seção `<TreeView.ContextMenu>` (linha ~191-200), substituir:

```xml
<ContextMenu>
    <MenuItem Header="Abrir em nova aba" Click="OnOpenInNewTab" />
    <MenuItem Header="Renomear (F2)" Click="OnRename" />
    <MenuItem Header="Excluir" Click="OnDelete" />
    <Separator />
    <MenuItem Header="Revelar no Explorer" Click="OnRevealInExplorer" />
    <MenuItem Header="Copiar caminho" Click="OnCopyPath" />
</ContextMenu>
```

por:

```xml
<ContextMenu>
    <MenuItem Header="Novo arquivo" Click="OnNewFile" InputGestureText="Ctrl+N" />
    <MenuItem Header="Nova pasta" Click="OnNewFolder" InputGestureText="Ctrl+Shift+N" />
    <Separator />
    <MenuItem Header="Abrir em nova aba" Click="OnOpenInNewTab" />
    <MenuItem Header="Renomear (F2)" Click="OnRename" />
    <MenuItem Header="Excluir" Click="OnDelete" InputGestureText="Del" />
    <Separator />
    <MenuItem Header="Copiar" Click="OnCopy" InputGestureText="Ctrl+C" />
    <MenuItem Header="Recortar" Click="OnCut" InputGestureText="Ctrl+X" />
    <MenuItem Header="Colar" Click="OnPaste" InputGestureText="Ctrl+V" />
    <Separator />
    <MenuItem Header="Revelar no Explorer" Click="OnRevealInExplorer" />
    <MenuItem Header="Copiar caminho" Click="OnCopyPath" />
</ContextMenu>
```

Nota: nesse step os handlers `OnNewFolder`, `OnCopy`, `OnCut`, `OnPaste` ainda não existem — Build vai falhar até implementar. Vamos adicionar stubs em Step 3 pra build não quebrar e depois preencher.

- [ ] **Step 3: Adicionar stubs dos outros handlers (pra build não quebrar)**

Em `ExplorerPanel.xaml.cs`, adicionar perto dos outros:

```csharp
private void OnNewFolder(object sender, RoutedEventArgs e) { /* impl Task 3.3 */ }
private void OnCopy(object sender, RoutedEventArgs e) { /* impl Task 4.x */ }
private void OnCut(object sender, RoutedEventArgs e) { /* impl Task 4.x */ }
private void OnPaste(object sender, RoutedEventArgs e) { /* impl Task 4.x */ }
```

- [ ] **Step 4: Build**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

Expected: 0 erros.

- [ ] **Step 5: Smoke test manual rápido**

```powershell
rtk dotnet run --project src/MeuMarkdown/MeuMarkdown.csproj -- D:\tmp\mm-v18-smoke\index.md
```

Right-click em pasta no Explorer → "Novo arquivo" → dialog aparece com `novo-arquivo.md` pré-preenchido → Enter → arquivo criado e abre em nova aba.

- [ ] **Step 6: Commit**

Run:
```powershell
rtk git add src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs
rtk git commit -m "feat(explorer): Novo arquivo via context menu + dialog de nome"
```

### Task 3.3: Handler `OnNewFolder`

**Files:**
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs`

- [ ] **Step 1: Substituir stub do `OnNewFolder`**

Substituir o stub:

```csharp
private void OnNewFolder(object sender, RoutedEventArgs e) { /* impl Task 3.3 */ }
```

por:

```csharp
private void OnNewFolder(object sender, RoutedEventArgs e)
{
    var targetDir = GetTargetDirectory();
    if (targetDir == null) return;

    var suggested = FileOperationsService.GenerateUniqueName(targetDir, "nova-pasta", null);
    var owner = Window.GetWindow(this);
    if (owner == null) return;

    var name = InputDialog.Show(owner, "Nova pasta",
        $"Nome da nova pasta em '{Path.GetFileName(targetDir)}':", suggested);
    if (string.IsNullOrWhiteSpace(name)) return;

    try
    {
        _fileOps.CreateNewFolder(targetDir, name);
        _workspaceService?.Refresh();
    }
    catch (FileOperationException ex)
    {
        MessageBox.Show(ex.Message, "Erro", MessageBoxButton.OK, MessageBoxImage.Error);
    }
}
```

- [ ] **Step 2: Build + smoke test**

```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

- [ ] **Step 3: Commit**

```powershell
rtk git add src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs
rtk git commit -m "feat(explorer): Nova pasta via context menu"
```

### Task 3.4: Atalhos `Ctrl+N` e `Ctrl+Shift+N`

**Files:**
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml`
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs`

- [ ] **Step 1: Declarar RoutedUICommands no code-behind**

Em `ExplorerPanel.xaml.cs`, perto do início da classe (após os fields):

```csharp
public static readonly RoutedUICommand NewFileCommand = new("Novo arquivo", "NewFile", typeof(ExplorerPanel));
public static readonly RoutedUICommand NewFolderCommand = new("Nova pasta", "NewFolder", typeof(ExplorerPanel));
public static readonly RoutedUICommand CopyCommand = new("Copiar", "CopyEx", typeof(ExplorerPanel));
public static readonly RoutedUICommand CutCommand = new("Recortar", "CutEx", typeof(ExplorerPanel));
public static readonly RoutedUICommand PasteCommand = new("Colar", "PasteEx", typeof(ExplorerPanel));
public static readonly RoutedUICommand DeleteCommand = new("Excluir", "DeleteEx", typeof(ExplorerPanel));
```

Adicionar `using System.Windows.Input;` se faltar.

- [ ] **Step 2: Bindings no XAML do TreeView**

Em `ExplorerPanel.xaml`, dentro do `<TreeView>` (após os outros bindings/handlers), adicionar:

```xml
<TreeView.InputBindings>
    <KeyBinding Modifiers="Ctrl" Key="N" Command="{x:Static local:ExplorerPanel.NewFileCommand}" />
    <KeyBinding Modifiers="Ctrl+Shift" Key="N" Command="{x:Static local:ExplorerPanel.NewFolderCommand}" />
    <KeyBinding Modifiers="Ctrl" Key="C" Command="{x:Static local:ExplorerPanel.CopyCommand}" />
    <KeyBinding Modifiers="Ctrl" Key="X" Command="{x:Static local:ExplorerPanel.CutCommand}" />
    <KeyBinding Modifiers="Ctrl" Key="V" Command="{x:Static local:ExplorerPanel.PasteCommand}" />
    <KeyBinding Key="Delete" Command="{x:Static local:ExplorerPanel.DeleteCommand}" />
</TreeView.InputBindings>
```

Adicionar namespace `xmlns:local="clr-namespace:MeuMarkdown.Controls.Panels"` no `<UserControl>` se ainda não tiver.

- [ ] **Step 3: CommandBindings no construtor do code-behind**

Em `ExplorerPanel.xaml.cs`, no construtor (após `InitializeComponent()`):

```csharp
CommandBindings.Add(new CommandBinding(NewFileCommand, OnNewFile));
CommandBindings.Add(new CommandBinding(NewFolderCommand, OnNewFolder));
CommandBindings.Add(new CommandBinding(CopyCommand, OnCopy));
CommandBindings.Add(new CommandBinding(CutCommand, OnCut));
CommandBindings.Add(new CommandBinding(PasteCommand, OnPaste));
CommandBindings.Add(new CommandBinding(DeleteCommand, OnDelete));
```

- [ ] **Step 4: Build**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

Expected: 0 erros.

- [ ] **Step 5: Commit**

```powershell
rtk git add src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs
rtk git commit -m "feat(explorer): atalhos Ctrl+N, Ctrl+Shift+N, Del e bindings para C/X/V"
```

---

## Fase 4 — Copy/Cut/Paste via Windows clipboard

### Task 4.1: Handlers `OnCopy` e `OnCut`

**Files:**
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs`

- [ ] **Step 1: Substituir stubs**

Substituir:

```csharp
private void OnCopy(object sender, RoutedEventArgs e) { /* impl Task 4.x */ }
private void OnCut(object sender, RoutedEventArgs e) { /* impl Task 4.x */ }
```

por:

```csharp
private const string CutFlagFormat = "Preferred DropEffect";

private void OnCopy(object sender, RoutedEventArgs e)
{
    if (FileTree.SelectedItem is not FileNode node) return;
    SetClipboardForFile(node.FullPath, cut: false);
}

private void OnCut(object sender, RoutedEventArgs e)
{
    if (FileTree.SelectedItem is not FileNode node) return;
    SetClipboardForFile(node.FullPath, cut: true);
}

private static void SetClipboardForFile(string path, bool cut)
{
    try
    {
        var data = new DataObject();
        data.SetData(DataFormats.FileDrop, new[] { path });
        data.SetData(DataFormats.Text, path);
        if (cut)
        {
            // DROPEFFECT_MOVE = 2 — compatível com Windows Explorer.
            var flag = new byte[] { 2, 0, 0, 0 };
            data.SetData(CutFlagFormat, new System.IO.MemoryStream(flag));
        }
        Clipboard.SetDataObject(data, copy: true);
    }
    catch (Exception ex)
    {
        MessageBox.Show($"Erro ao acessar clipboard:\n{ex.Message}",
            "Erro", MessageBoxButton.OK, MessageBoxImage.Error);
    }
}
```

- [ ] **Step 2: Build**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

- [ ] **Step 3: Commit**

```powershell
rtk git add src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs
rtk git commit -m "feat(explorer): Copy/Cut envia FileDrop + flag DROPEFFECT pro clipboard"
```

### Task 4.2: Handler `OnPaste`

**Files:**
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs`

- [ ] **Step 1: Substituir stub**

Substituir:

```csharp
private void OnPaste(object sender, RoutedEventArgs e) { /* impl Task 4.x */ }
```

por:

```csharp
private void OnPaste(object sender, RoutedEventArgs e)
{
    var targetDir = GetTargetDirectory();
    if (targetDir == null) return;

    try
    {
        var data = Clipboard.GetDataObject();
        if (data?.GetDataPresent(DataFormats.FileDrop) != true) return;

        var paths = (string[])data.GetData(DataFormats.FileDrop);
        if (paths == null || paths.Length == 0) return;
        var sourcePath = paths[0]; // YAGNI: 1 item por vez

        if (!System.IO.File.Exists(sourcePath))
        {
            MessageBox.Show("Arquivo do clipboard não existe mais.", "Erro",
                MessageBoxButton.OK, MessageBoxImage.Warning);
            return;
        }

        // Detectar Cut vs Copy pela flag.
        bool isCut = false;
        if (data.GetDataPresent(CutFlagFormat) &&
            data.GetData(CutFlagFormat) is System.IO.MemoryStream ms)
        {
            var bytes = ms.ToArray();
            isCut = bytes.Length > 0 && bytes[0] == 2;
        }

        string? result = isCut
            ? _fileOps.MoveFile(sourcePath, targetDir, AskOverwrite)
            : _fileOps.CopyFile(sourcePath, targetDir, AskOverwrite);

        if (result != null)
        {
            _workspaceService?.Refresh();
            if (isCut)
            {
                // Clipboard fica vazio após Cut (comportamento Windows Explorer).
                Clipboard.Clear();
            }
        }
    }
    catch (FileOperationException ex)
    {
        MessageBox.Show(ex.Message, "Erro", MessageBoxButton.OK, MessageBoxImage.Error);
    }
    catch (Exception ex)
    {
        MessageBox.Show($"Erro inesperado ao colar:\n{ex.Message}",
            "Erro", MessageBoxButton.OK, MessageBoxImage.Error);
    }
}

private bool AskOverwrite(string existingPath)
{
    var name = Path.GetFileName(existingPath);
    var result = MessageBox.Show(
        $"'{name}' já existe no destino.\n\nSobrescrever?",
        "Confirmar sobrescrita",
        MessageBoxButton.YesNo, MessageBoxImage.Warning);
    return result == MessageBoxResult.Yes;
}
```

- [ ] **Step 2: Build**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

- [ ] **Step 3: Smoke test manual**

```powershell
rtk dotnet run --project src/MeuMarkdown/MeuMarkdown.csproj
```

- Right-click arquivo → Copiar → outra pasta → Right-click → Colar → cópia criada
- Right-click arquivo → Recortar → outra pasta → Right-click → Colar → arquivo MOVIDO
- Copiar arquivo do Windows Explorer (Ctrl+C) → no Meu Markdown Ctrl+V → arquivo aparece

- [ ] **Step 4: Commit**

```powershell
rtk git add src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs
rtk git commit -m "feat(explorer): Paste com detecção de Cut/Copy + AskOverwrite"
```

---

## Fase 5 — Drag-and-drop

### Task 5.1: Drag start (PreviewMouseLeft + PreviewMouseMove)

**Files:**
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml`
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs`

- [ ] **Step 1: Adicionar event handlers no XAML do TreeView**

Em `ExplorerPanel.xaml`, no `<TreeView>` existente, adicionar:

```xml
PreviewMouseLeftButtonDown="OnTreePreviewLeftButtonDown"
PreviewMouseMove="OnTreePreviewMouseMove"
AllowDrop="True"
DragOver="OnTreeDragOver"
Drop="OnTreeDrop"
```

(O `PreviewMouseRightButtonDown` já existe do v1.8.0 — manter.)

- [ ] **Step 2: Adicionar fields + handlers no code-behind**

Em `ExplorerPanel.xaml.cs`, adicionar fields:

```csharp
private System.Windows.Point _dragStartPoint;
private FileNode? _dragSourceNode;
```

E handlers (perto do `OnTreePreviewRightButtonDown`):

```csharp
private void OnTreePreviewLeftButtonDown(object sender, MouseButtonEventArgs e)
{
    _dragStartPoint = e.GetPosition(null);
    _dragSourceNode = FindFileNode(e.OriginalSource);
}

private void OnTreePreviewMouseMove(object sender, MouseEventArgs e)
{
    if (e.LeftButton != MouseButtonState.Pressed) return;
    if (_dragSourceNode == null) return;

    var diff = _dragStartPoint - e.GetPosition(null);
    if (Math.Abs(diff.X) < SystemParameters.MinimumHorizontalDragDistance &&
        Math.Abs(diff.Y) < SystemParameters.MinimumVerticalDragDistance) return;

    try
    {
        var data = new DataObject(DataFormats.FileDrop, new[] { _dragSourceNode.FullPath });
        var effect = (Keyboard.Modifiers & ModifierKeys.Control) != 0
            ? DragDropEffects.Copy
            : DragDropEffects.Move;
        DragDrop.DoDragDrop(FileTree, data, effect);
    }
    finally
    {
        _dragSourceNode = null;
    }
}

private static FileNode? FindFileNode(object originalSource)
{
    var dep = originalSource as System.Windows.DependencyObject;
    while (dep != null && dep is not TreeViewItem)
    {
        dep = System.Windows.Media.VisualTreeHelper.GetParent(dep);
    }
    return (dep as TreeViewItem)?.DataContext as FileNode;
}
```

- [ ] **Step 3: Stubs dos handlers de drag-over/drop (Task 5.2 implementa)**

```csharp
private void OnTreeDragOver(object sender, DragEventArgs e) { /* Task 5.2 */ }
private void OnTreeDrop(object sender, DragEventArgs e) { /* Task 5.2 */ }
```

- [ ] **Step 4: Build**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

- [ ] **Step 5: Commit**

```powershell
rtk git add src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs
rtk git commit -m "feat(explorer): drag start handler com FindFileNode helper"
```

### Task 5.2: DragOver + Drop

**Files:**
- Modify: `src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs`

- [ ] **Step 1: Substituir stubs**

Substituir:

```csharp
private void OnTreeDragOver(object sender, DragEventArgs e) { /* Task 5.2 */ }
private void OnTreeDrop(object sender, DragEventArgs e) { /* Task 5.2 */ }
```

por:

```csharp
private void OnTreeDragOver(object sender, DragEventArgs e)
{
    e.Effects = DragDropEffects.None;
    e.Handled = true;

    if (!e.Data.GetDataPresent(DataFormats.FileDrop)) return;

    var target = FindFileNode(e.OriginalSource);
    if (target?.IsDirectory != true) return;

    e.Effects = (e.KeyStates & DragDropKeyStates.ControlKey) != 0
        ? DragDropEffects.Copy
        : DragDropEffects.Move;
}

private void OnTreeDrop(object sender, DragEventArgs e)
{
    e.Handled = true;

    var target = FindFileNode(e.OriginalSource);
    if (target?.IsDirectory != true) return;

    if (e.Data.GetData(DataFormats.FileDrop) is not string[] sources || sources.Length == 0) return;
    var sourcePath = sources[0];

    // Cancela drops degenerados
    if (string.Equals(Path.GetDirectoryName(sourcePath), target.FullPath, StringComparison.OrdinalIgnoreCase))
        return; // arquivo já está dentro da pasta
    if (string.Equals(sourcePath, target.FullPath, StringComparison.OrdinalIgnoreCase))
        return; // drop em si mesmo

    var isCopy = (e.KeyStates & DragDropKeyStates.ControlKey) != 0;

    try
    {
        string? result = isCopy
            ? _fileOps.CopyFile(sourcePath, target.FullPath, AskOverwrite)
            : _fileOps.MoveFile(sourcePath, target.FullPath, AskOverwrite);

        if (result != null) _workspaceService?.Refresh();
    }
    catch (FileOperationException ex)
    {
        MessageBox.Show(ex.Message, "Erro", MessageBoxButton.OK, MessageBoxImage.Error);
    }
}
```

- [ ] **Step 2: Build + smoke test**

```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

Smoke test:
- Arrastar arquivo pra outra pasta → cursor mostra "Move", solta → arquivo MOVIDO
- Segurar Ctrl + arrastar → cursor mostra "+", solta → arquivo COPIADO
- Arrastar pra arquivo (não pasta) → cursor mostra "proibido", drop não acontece

- [ ] **Step 3: Commit**

```powershell
rtk git add src/MeuMarkdown/Controls/Panels/ExplorerPanel.xaml.cs
rtk git commit -m "feat(explorer): drag-over e drop com move/copy via Ctrl"
```

---

## Fase 6 — Fechar abas órfãs ao trocar workspace

### Task 6.1: Método `CloseTabsOutsideWorkspace` no `MainViewModel`

**Files:**
- Modify: `src/MeuMarkdown/ViewModels/MainViewModel.cs`

- [ ] **Step 1: Adicionar método**

Em `MainViewModel.cs`, adicionar após `OnNavigationRequested` ou perto dos outros métodos de Tab:

```csharp
/// <summary>
/// Fecha todas as abas cujo arquivo não está dentro do newWorkspaceRoot.
/// Abas dirty pedem confirmação via TryCloseTab existente; se user cancela,
/// aquela aba específica permanece, mas as outras continuam fechando.
/// </summary>
public void CloseTabsOutsideWorkspace(string? newWorkspaceRoot)
{
    if (string.IsNullOrEmpty(newWorkspaceRoot)) return;

    var tabsToClose = Tabs
        .Where(t => !IsUnderRoot(t.FilePath, newWorkspaceRoot))
        .ToList();

    foreach (var tab in tabsToClose)
    {
        if (!TryCloseTab(tab))
        {
            // User cancelou na prompt de save — para o lote.
            // (Outras abas no lote já foram processadas se vieram antes.)
            break;
        }
    }
}

private static bool IsUnderRoot(string filePath, string root)
{
    if (string.IsNullOrEmpty(filePath) || string.IsNullOrEmpty(root)) return false;
    try
    {
        var normalizedFile = Path.GetFullPath(filePath);
        var normalizedRoot = Path.GetFullPath(root).TrimEnd(Path.DirectorySeparatorChar)
            + Path.DirectorySeparatorChar;
        return normalizedFile.StartsWith(normalizedRoot, StringComparison.OrdinalIgnoreCase);
    }
    catch
    {
        return false;
    }
}
```

Se faltar `using System.IO;`, adicionar.

- [ ] **Step 2: Build**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

Expected: 0 erros.

### Task 6.2: Chamar `CloseTabsOutsideWorkspace` no `OnOpenFolderRequested`

**Files:**
- Modify: `src/MeuMarkdown/MainWindow.xaml.cs`

- [ ] **Step 1: Encontrar o handler `OnOpenFolderRequested`**

Em `MainWindow.xaml.cs`, localizar o handler (linha ~1248-1270).

- [ ] **Step 2: Modificar pra fechar abas órfãs após Open**

Substituir o corpo do handler. Estrutura esperada antes:

```csharp
private void OnOpenFolderRequested(object? sender, EventArgs e)
{
    var dlg = new Microsoft.Win32.OpenFolderDialog
    {
        Title = "Abrir pasta como workspace"
    };
    if (dlg.ShowDialog() == true)
    {
        _viewModel.WorkspaceService.Open(dlg.FolderName,
            App.State.Preferences.ExplorerShowAllFiles);
        // ... (resto do código existente — pode ter mais coisas como atualizar UI)
    }
}
```

Adicionar a chamada de fechamento de abas órfãs **após** o `Open()`:

```csharp
private void OnOpenFolderRequested(object? sender, EventArgs e)
{
    var dlg = new Microsoft.Win32.OpenFolderDialog
    {
        Title = "Abrir pasta como workspace"
    };
    if (dlg.ShowDialog() == true)
    {
        _viewModel.WorkspaceService.Open(dlg.FolderName,
            App.State.Preferences.ExplorerShowAllFiles);
        _viewModel.CloseTabsOutsideWorkspace(dlg.FolderName);
        // ... (resto do código existente)
    }
}
```

Se o handler tem outra estrutura, ajustar pra que a chamada `CloseTabsOutsideWorkspace` venha após `Open`.

- [ ] **Step 3: Build**

Run:
```powershell
rtk dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Debug --nologo -v quiet 2>&1 | rtk tail -n 3
```

- [ ] **Step 4: Smoke test manual**

```powershell
rtk dotnet run --project src/MeuMarkdown/MeuMarkdown.csproj
```

- Abrir workspace A com 3 abas
- Trocar pra workspace B (totalmente diferente) → as 3 abas fecham automaticamente
- Reabrir workspace A (mesmo path) → nenhuma aba aberta (state.json não restaura)
- Abrir 2 abas em workspace A; trocar pra workspace C que CONTÉM 1 das abas → 1 fecha, 1 permanece

- [ ] **Step 5: Commit**

```powershell
rtk git add src/MeuMarkdown/ViewModels/MainViewModel.cs src/MeuMarkdown/MainWindow.xaml.cs
rtk git commit -m "feat(workspace): fecha abas orfas ao trocar workspace"
```

---

## Fase 7 — Docs

### Task 7.1: Atualizar CHANGELOG.md

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Adicionar entradas em `[Não lançado]`**

Em `CHANGELOG.md` substituir:

```markdown
## [Não lançado]

## [1.8.0] — 2026
```

por:

```markdown
## [Não lançado]

### Adicionado
- **Operações de arquivo no Explorer**:
  - Novo arquivo `.md` (`Ctrl+N`) e nova pasta (`Ctrl+Shift+N`) — abre dialog
    com nome sugerido pré-preenchido; Enter cria + abre em aba (arquivo).
  - Copiar/Recortar/Colar (`Ctrl+C/X/V`) usando Windows clipboard — compatível
    com Windows Explorer (copia em um, cola em outro).
  - Drag-and-drop entre diretórios — `Ctrl+drag` para copiar (move por padrão).

### Corrigido
- Abas órfãs ao trocar workspace: arquivos do workspace anterior agora fecham
  automaticamente quando o novo workspace não os contém. Abas com mudanças não
  salvas pedem confirmação antes de fechar.

## [1.8.0] — 2026
```

- [ ] **Step 2: Commit**

```powershell
rtk git add CHANGELOG.md
rtk git commit -m "docs(changelog): entradas para v1.9.0"
```

### Task 7.2: Atualizar README.md

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Adicionar linha na grid de funcionalidades**

Em `README.md`, na grid de funcionalidades (perto da linha 47), adicionar após a última linha:

```markdown
| 🗂️ **Operações de arquivo** no Explorer (novo arquivo/pasta, copy/paste, drag-and-drop) | | |
```

(Deixar células 2 e 3 vazias se a grid for de 3 colunas.)

- [ ] **Step 2: Atualizar Roadmap → "Já entregue"**

Substituir `### ✅ Já entregue (v1.8.0)` por `### ✅ Já entregue (v1.9.0)` e adicionar bullet:

```markdown
- Operações de arquivo no Explorer (novo, copy/cut/paste, drag-and-drop) com atalhos
```

- [ ] **Step 3: Atualizar seção "Atalhos" se quiser destacar (opcional)**

Adicionar bullets pros atalhos `Ctrl+N`, `Ctrl+Shift+N`, `Ctrl+C/X/V`, `Del` na seção apropriada.

- [ ] **Step 4: Commit**

```powershell
rtk git add README.md
rtk git commit -m "docs(readme): destaca operacoes de arquivo do v1.9.0"
```

### Task 7.3: Wiki — atualizar atalhos

**Files:**
- Modify: `D:\src\meu-markdown.wiki\Atalhos-de-teclado.md`
- Modify (opcional): `D:\src\meu-markdown.wiki\Funcionalidades.md`

- [ ] **Step 1: Adicionar nova seção "Explorer" em Atalhos-de-teclado.md**

Antes da seção "Modos de foco", adicionar:

```markdown
## Explorer

| Atalho | Ação |
|---|---|
| `Ctrl+N` | Novo arquivo `.md` |
| `Ctrl+Shift+N` | Nova pasta |
| `Ctrl+C` | Copiar arquivo selecionado |
| `Ctrl+X` | Recortar arquivo selecionado |
| `Ctrl+V` | Colar (no diretório selecionado ou no root) |
| `Del` | Excluir arquivo/pasta selecionado |

> [!TIP]
> Arrastar arquivos entre pastas no Explorer **move** por padrão. Segure `Ctrl` enquanto arrasta pra **copiar**.

```

- [ ] **Step 2: Commit (na wiki)**

```powershell
Push-Location D:\src\meu-markdown.wiki
rtk git add Atalhos-de-teclado.md
rtk git commit -m "docs(wiki): adiciona secao Explorer (operacoes de arquivo v1.9.0)"
Pop-Location
```

(Push da wiki na Fase 8, junto com o release.)

---

## Fase 8 — Smoke test + Release

### Task 8.1: Plano de testes manuais

**Files:**
- Create: `docs/test-plans/v1.9.0.md`

- [ ] **Step 1: Criar arquivo**

Copiar a seção "Plano de testes manuais" do spec ([docs/specs/2026-05-20-v1.9.0-design.md](../specs/2026-05-20-v1.9.0-design.md)) para `docs/test-plans/v1.9.0.md`. Topo:

```markdown
# v1.9.0 — Plano de testes manuais

Rodar **contra o instalador release** (não apenas Debug).

- Data do teste: ___________
- Versão testada: _______
- Testador: _________

```

- [ ] **Step 2: Commit**

```powershell
rtk git add docs/test-plans/v1.9.0.md
rtk git commit -m "docs: plano de testes manuais v1.9.0"
```

### Task 8.2: Suite verde + smoke build

- [ ] **Step 1: xUnit suite completa**

Run:
```powershell
rtk dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --nologo -v minimal 2>&1 | rtk tail -n 5
```

Expected: ~130 testes verdes (114 anteriores + 16 novos do FileOperationsService).

- [ ] **Step 2: Release build single-file**

```powershell
rtk dotnet publish src/MeuMarkdown/MeuMarkdown.csproj -c Release --nologo -v quiet 2>&1 | rtk tail -n 5
(Get-Item src\MeuMarkdown\bin\Release\net10.0-windows\win-x64\publish\MeuMarkdown.exe).Length / 1MB
```

Expected: PASS, exe ~170MB (sem variação significativa do v1.8.0 — só código novo, sem libs novas).

- [ ] **Step 3: Smoke test interativo**

Rodar o app e validar **cada item** do plano de testes manuais (`docs/test-plans/v1.9.0.md`).

- [ ] **Step 4: Mergear feat/v1.9.0 → main + push**

Após smoke OK:

```powershell
rtk git checkout main
rtk git merge --no-ff feat/v1.9.0 -m "Merge branch 'feat/v1.9.0' — file operations Explorer + close orphan tabs"
rtk git push origin main
```

### Task 8.3: Disparar release via skill `/release`

- [ ] **Step 1: Confirmar working tree limpo**

```powershell
rtk git status
```

Expected: clean, `.claude/settings.json` ignorável.

- [ ] **Step 2: Invocar a skill**

Rodar no Claude Code: `/release 1.9.0`.

A skill faz: gate de qualidade (build+test), audit de docs, bump em `Directory.Build.props`, renomear `[Não lançado]` → `[1.9.0]` no CHANGELOG, commit `chore: release v1.9.0`, tag `v1.9.0`, push (dispara GitHub Actions).

- [ ] **Step 3: Acompanhar workflow**

```powershell
rtk gh run watch --exit-status
```

Expected: workflow `release.yml` builda, gera instalador, anexa assets, cria GitHub Release. ~3-5min.

- [ ] **Step 4: Pushar a wiki**

```powershell
Push-Location D:\src\meu-markdown.wiki
rtk git push
Pop-Location
```

- [ ] **Step 5: Validar release publicada**

```powershell
rtk gh release view v1.9.0
```

Expected: release com notes do CHANGELOG seção `[1.9.0]`, 2 assets (instalador + portable).

---

## Self-Review

(executada pela skill após escrever o plano)

**1. Spec coverage:**

| Item do spec | Task(s) |
|---|---|
| `FileOperationsService.GenerateUniqueName` | 1.1 |
| `CreateNewFile` / `CreateNewFolder` | 1.2 |
| `CopyFile` / `MoveFile` com `onConflict` | 1.3 |
| `InputDialog` reutilizável | 2.1 |
| `OnNewFile` handler + UX dialog | 3.2 |
| `OnNewFolder` handler | 3.3 |
| Atalhos `Ctrl+N`, `Ctrl+Shift+N`, `Ctrl+C/X/V`, `Del` | 3.4 |
| `OnCopy` / `OnCut` com clipboard `FileDrop` + DROPEFFECT | 4.1 |
| `OnPaste` com detecção de cut/copy | 4.2 |
| Drag start (drag-and-drop iniciador) | 5.1 |
| Drag-over + Drop (target receiver) | 5.2 |
| `CloseTabsOutsideWorkspace` no `MainViewModel` | 6.1 |
| Chamada do close em `OnOpenFolderRequested` | 6.2 |
| CHANGELOG `[Não lançado]` | 7.1 |
| README grid + roadmap | 7.2 |
| Wiki atalhos | 7.3 |
| Plano de testes manuais | 8.1 |
| Smoke + release | 8.2, 8.3 |

Sem gaps.

**2. Placeholder scan:** `/* impl Task X */` aparece em alguns stubs intencionais (Task 3.2 step 3, Task 5.1 step 3) — são preenchidos em tasks subsequentes explícitas. Não são placeholders escondidos.

**3. Type consistency:**

- `FileOperationsService.GenerateUniqueName(string, string, string?)` — `extension` é nullable em todos os usos ✓
- `CreateNewFile(directory, baseName, extension)` — assinatura coerente entre service (1.2) e callers (3.2) ✓
- `CopyFile/MoveFile(source, destDir, onConflict?)` — `onConflict` é `Func<string, bool>?` consistente ✓
- `InputDialog.Show(owner, title, prompt, defaultValue) → string?` — consistente entre 2.1 (definição) e 3.2/3.3 (uso) ✓
- `RoutedUICommand` declarados em 3.4 step 1 — bindados em 3.4 step 3 com `CommandBinding(NewFileCommand, OnNewFile)` (assinatura compatível com `OnNewFile(object, RoutedEventArgs)` — sim ✓)
- `IsUnderRoot(filePath, root)` é static — usado uma vez em 6.1 ✓
- `CutFlagFormat` é constante usada em 4.1 (set) e 4.2 (read) — string `"Preferred DropEffect"` ✓
- `AskOverwrite(string) → bool` — assinatura compatível com `Func<string, bool>` ✓
- `FindFileNode(originalSource)` — usado em 5.1 (drag start) e 5.2 (drag-over/drop) consistente ✓

Nenhuma inconsistência.
