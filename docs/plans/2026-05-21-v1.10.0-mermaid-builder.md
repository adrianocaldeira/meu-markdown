# v1.10.0 — Construtor Visual de Mermaid: Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar menu "Inserir > Mermaid" com 8 templates de diagrama e janela construtor visual cobrindo Flowchart e Sequence, com preview ao vivo.

**Architecture:** WPF nativo + CommunityToolkit.Mvvm. Modelos puros (`FlowchartModel`/`SequenceModel`) com `ToMermaid()` testável fora de WPF. Janela `MermaidBuilderWindow` com layout split: formulário à esquerda (UserControl por tipo), `MermaidPreviewLite` (WebView2 mínima) à direita. Inserção no AvalonEdit feita via helper estático puro (`MarkdownInsertionService.BuildMermaidInsertion`) chamado de `MainWindow.InsertMermaidBlock` — segue o padrão existente de `WrapSelection`/`InsertAtCursor`.

**Tech Stack:** .NET 10 WPF, CommunityToolkit.Mvvm 8.4, AvalonEdit 6.3, WebView2 1.0.3800, Markdig 1.1, Mermaid (já embedded em `Resources/mermaid.min.js`), xUnit 2.9.

**Spec:** [`docs/specs/2026-05-21-v1.10.0-mermaid-builder-design.md`](../specs/2026-05-21-v1.10.0-mermaid-builder-design.md)

---

## Divergências aprovadas vs spec

- **Inserção no editor**: spec previa `DocumentTabViewModel.InsertMermaidBlock`. Implementação coloca o método em `MainWindow.xaml.cs` para consistência com o padrão existente (`WrapSelection`, `InsertAtCursor`, `InsertLink`, `InsertLinePrefix`) que vivem no code-behind porque o `textEditor` (AvalonEdit) está no XAML do MainWindow. A lógica pura de cálculo de prefixo/sufixo/caret vai para um helper estático em `Services/MarkdownInsertionService.cs` — testável sem WPF.
- **Virtual host do preview lite**: reaproveita o `assetsDir` (`%LocalAppData%/MeuMarkdown/webview-assets/`) já populado por `MarkdownService.ExtractEnhancementAssetsTo`. A janela construtor recebe esse path do `MainWindow` ao abrir.

---

## File Structure

### Novos arquivos

| Caminho | Responsabilidade |
|---|---|
| `src/MeuMarkdown/Models/Mermaid/MermaidDiagramType.cs` | Enum: Flowchart, Sequence, Class, Er, State, Gantt, Mindmap, Pie. |
| `src/MeuMarkdown/Models/Mermaid/FlowDirection.cs` | Enum: TD, LR, BT, RL. |
| `src/MeuMarkdown/Models/Mermaid/FlowNodeShape.cs` | Enum: Rectangle, Rounded, Diamond, Circle, Stadium. |
| `src/MeuMarkdown/Models/Mermaid/FlowArrowType.cs` | Enum: Solid, SolidLabeled, Dotted, Thick. |
| `src/MeuMarkdown/Models/Mermaid/FlowNode.cs` | ObservableObject: Id, Label, Shape. |
| `src/MeuMarkdown/Models/Mermaid/FlowEdge.cs` | ObservableObject: FromId, ToId, Arrow, Label. |
| `src/MeuMarkdown/Models/Mermaid/FlowchartModel.cs` | ObservableObject: Direction, Nodes, Edges, `ToMermaid()`. |
| `src/MeuMarkdown/Models/Mermaid/SequenceArrowType.cs` | Enum: Sync, Async, Reply. |
| `src/MeuMarkdown/Models/Mermaid/Actor.cs` | ObservableObject: Name, Alias, IsActor. |
| `src/MeuMarkdown/Models/Mermaid/Message.cs` | ObservableObject: FromActor, ToActor, Arrow, Label. |
| `src/MeuMarkdown/Models/Mermaid/SequenceModel.cs` | ObservableObject: Actors, Messages, `ToMermaid()`. |
| `src/MeuMarkdown/Services/MermaidTemplateService.cs` | Carrega templates de `Resources/MermaidTemplates/*.txt`, cache por tipo. |
| `src/MeuMarkdown/Services/MarkdownInsertionService.cs` | Helper estático puro: `BuildMermaidInsertion(content, caretOffset, code) → (text, newOffset)`. |
| `src/MeuMarkdown/ViewModels/MermaidBuilderViewModel.cs` | Orquestra a janela construtor. |
| `src/MeuMarkdown/Views/MermaidBuilderWindow.xaml(.cs)` | Janela WPF do construtor (split). |
| `src/MeuMarkdown/Controls/MermaidBuilderPanels/FlowchartBuilderPanel.xaml(.cs)` | UserControl com formulário de Flowchart. |
| `src/MeuMarkdown/Controls/MermaidBuilderPanels/SequenceBuilderPanel.xaml(.cs)` | UserControl com formulário de Sequence. |
| `src/MeuMarkdown/Controls/MermaidPreviewLite.xaml(.cs)` | UserControl WebView2 mínimo com `RenderAsync(string)`. |
| `src/MeuMarkdown/Resources/mermaid-preview-lite.html` | HTML mínimo do preview lite. EmbeddedResource. |
| `src/MeuMarkdown/Resources/MermaidTemplates/flowchart-td.txt` | Template Fluxograma TD. EmbeddedResource. |
| `src/MeuMarkdown/Resources/MermaidTemplates/sequence.txt` | Template Sequência. EmbeddedResource. |
| `src/MeuMarkdown/Resources/MermaidTemplates/class.txt` | Template Classe. EmbeddedResource. |
| `src/MeuMarkdown/Resources/MermaidTemplates/er.txt` | Template ER. EmbeddedResource. |
| `src/MeuMarkdown/Resources/MermaidTemplates/state.txt` | Template Estado. EmbeddedResource. |
| `src/MeuMarkdown/Resources/MermaidTemplates/gantt.txt` | Template Gantt. EmbeddedResource. |
| `src/MeuMarkdown/Resources/MermaidTemplates/mindmap.txt` | Template Mindmap. EmbeddedResource. |
| `src/MeuMarkdown/Resources/MermaidTemplates/pie.txt` | Template Pie. EmbeddedResource. |
| `tests/MeuMarkdown.Tests/Services/MermaidTemplateServiceTests.cs` | Testes do serviço de template. |
| `tests/MeuMarkdown.Tests/Services/MarkdownInsertionServiceTests.cs` | Testes do helper de inserção. |
| `tests/MeuMarkdown.Tests/Models/Mermaid/FlowchartModelTests.cs` | Testes do FlowchartModel.ToMermaid(). |
| `tests/MeuMarkdown.Tests/Models/Mermaid/SequenceModelTests.cs` | Testes do SequenceModel.ToMermaid(). |

### Arquivos modificados

| Caminho | Mudança |
|---|---|
| `Directory.Build.props` | Bump `<Version>` para `1.10.0`. |
| `src/MeuMarkdown/MeuMarkdown.csproj` | Adicionar `<EmbeddedResource>` para os 8 templates + `mermaid-preview-lite.html`. |
| `src/MeuMarkdown/MainWindow.xaml` | Adicionar `MenuItem "_Inserir"` entre Navegar e Exibir (linha ~264). |
| `src/MeuMarkdown/MainWindow.xaml.cs` | Adicionar handlers `OnInsertMermaidTemplate`, `OnOpenMermaidBuilder`, método `InsertMermaidBlock`. Passar `_assetsDir` ao abrir builder. |
| `CHANGELOG.md` | Entradas em `[Não lançado]` (movidas para `[1.10.0]` no release). |
| `README.md` | Seção "Já entregue (v1.10.0)" no fim do release. |

---

## Convenções para todas as tasks

- **Comandos**: usar PowerShell. Build: `dotnet build src/MeuMarkdown/MeuMarkdown.csproj`. Testes: `dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj`. Sempre prefixar com `rtk` no terminal interativo do usuário; em CI/scripts não é necessário.
- **TDD**: cada componente de lógica tem teste antes da implementação. Modelos WPF (XAML) e classes XAML são testados manualmente (smoke documentado).
- **Commits**: Conventional Commits em pt-BR. Um commit por task (ou logical group).
- **Idioma**: textos visíveis ao usuário em pt-BR; nomes de classe/método em inglês.

---

## Task 0 — Setup: branch e baseline verde

**Files:**
- Modify: `Directory.Build.props`

- [ ] **Step 1: Criar branch de feature**

```powershell
git checkout main
git pull --ff-only
git checkout -b feat/v1.10.0
```

- [ ] **Step 2: Confirmar baseline build verde**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded, 0 Error(s).

- [ ] **Step 3: Confirmar baseline tests verde**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj
```
Expected: Passed, 0 Failed.

- [ ] **Step 4: Bump de versão**

Editar `Directory.Build.props` e mudar `<Version>1.9.0</Version>` para `<Version>1.10.0</Version>` (ou rodar `pwsh scripts/bump-version.ps1 1.10.0 -NoCommit` se existir e for não-interativo).

- [ ] **Step 5: Commit**

```powershell
git add Directory.Build.props
git commit -m "chore: bump version para v1.10.0 (em desenvolvimento)"
```

---

## Task 1 — Enum `MermaidDiagramType` + arquivos de template

**Files:**
- Create: `src/MeuMarkdown/Models/Mermaid/MermaidDiagramType.cs`
- Create: `src/MeuMarkdown/Resources/MermaidTemplates/*.txt` (8 arquivos)
- Modify: `src/MeuMarkdown/MeuMarkdown.csproj`

- [ ] **Step 1: Criar enum `MermaidDiagramType`**

`src/MeuMarkdown/Models/Mermaid/MermaidDiagramType.cs`:
```csharp
namespace MeuMarkdown.Models.Mermaid;

public enum MermaidDiagramType
{
    Flowchart,
    Sequence,
    Class,
    Er,
    State,
    Gantt,
    Mindmap,
    Pie,
}
```

- [ ] **Step 2: Criar `flowchart-td.txt`**

```
graph TD
    %% Troque os labels e adicione mais nós conforme precisar
    A[Início] --> B[Processo]
    B --> C[Fim]
```

- [ ] **Step 3: Criar `sequence.txt`**

```
sequenceDiagram
    %% Atores e mensagens — troque os nomes e textos
    Alice->>Bob: Olá Bob
    Bob-->>Alice: Olá Alice
```

- [ ] **Step 4: Criar `class.txt`**

```
classDiagram
    %% Defina classes, campos, métodos e relações
    class Animal {
      +String nome
      +int idade
      +emitirSom() void
    }
    class Cachorro
    Animal <|-- Cachorro
```

- [ ] **Step 5: Criar `er.txt`**

```
erDiagram
    %% Entidades e relações — || (um) o{ (muitos)
    CLIENTE ||--o{ PEDIDO : faz
    PEDIDO {
      int id
      string descricao
    }
```

- [ ] **Step 6: Criar `state.txt`**

```
stateDiagram-v2
    %% Estados e transições
    [*] --> Ativo
    Ativo --> Inativo : desativar
    Inativo --> [*]
```

- [ ] **Step 7: Criar `gantt.txt`**

```
gantt
    title Cronograma
    dateFormat YYYY-MM-DD
    %% Adicione mais seções e tarefas conforme precisar
    section Planejamento
    Levantar requisitos :a1, 2026-06-01, 5d
    Desenhar solução    :a2, after a1, 3d
```

- [ ] **Step 8: Criar `mindmap.txt`**

```
mindmap
    %% Raiz e filhos — indentação define hierarquia
    root((Ideia central))
        Subtema A
            Detalhe 1
        Subtema B
        Subtema C
```

- [ ] **Step 9: Criar `pie.txt`**

```
pie title Distribuição
    %% Substitua labels e valores
    "Categoria A" : 40
    "Categoria B" : 35
    "Categoria C" : 25
```

- [ ] **Step 10: Registrar como EmbeddedResource no csproj**

Em `src/MeuMarkdown/MeuMarkdown.csproj`, dentro do `<ItemGroup>` que já tem `<EmbeddedResource>`, adicionar:
```xml
<EmbeddedResource Include="Resources\MermaidTemplates\flowchart-td.txt" />
<EmbeddedResource Include="Resources\MermaidTemplates\sequence.txt" />
<EmbeddedResource Include="Resources\MermaidTemplates\class.txt" />
<EmbeddedResource Include="Resources\MermaidTemplates\er.txt" />
<EmbeddedResource Include="Resources\MermaidTemplates\state.txt" />
<EmbeddedResource Include="Resources\MermaidTemplates\gantt.txt" />
<EmbeddedResource Include="Resources\MermaidTemplates\mindmap.txt" />
<EmbeddedResource Include="Resources\MermaidTemplates\pie.txt" />
```

- [ ] **Step 11: Build sanity check**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded.

- [ ] **Step 12: Commit**

```powershell
git add src/MeuMarkdown/Models/Mermaid/MermaidDiagramType.cs src/MeuMarkdown/Resources/MermaidTemplates src/MeuMarkdown/MeuMarkdown.csproj
git commit -m "feat(mermaid): templates de 8 tipos de diagrama como EmbeddedResource"
```

---

## Task 2 — `MermaidTemplateService` (TDD)

**Files:**
- Create: `src/MeuMarkdown/Services/MermaidTemplateService.cs`
- Test: `tests/MeuMarkdown.Tests/Services/MermaidTemplateServiceTests.cs`

- [ ] **Step 1: Escrever testes (falham)**

`tests/MeuMarkdown.Tests/Services/MermaidTemplateServiceTests.cs`:
```csharp
using MeuMarkdown.Models.Mermaid;
using MeuMarkdown.Services;

namespace MeuMarkdown.Tests.Services;

public class MermaidTemplateServiceTests
{
    [Theory]
    [InlineData(MermaidDiagramType.Flowchart, "graph TD")]
    [InlineData(MermaidDiagramType.Sequence, "sequenceDiagram")]
    [InlineData(MermaidDiagramType.Class, "classDiagram")]
    [InlineData(MermaidDiagramType.Er, "erDiagram")]
    [InlineData(MermaidDiagramType.State, "stateDiagram-v2")]
    [InlineData(MermaidDiagramType.Gantt, "gantt")]
    [InlineData(MermaidDiagramType.Mindmap, "mindmap")]
    [InlineData(MermaidDiagramType.Pie, "pie")]
    public void GetTemplate_StartsWithExpectedKeyword(MermaidDiagramType type, string keyword)
    {
        var svc = new MermaidTemplateService();
        var text = svc.GetTemplate(type);
        Assert.StartsWith(keyword, text.TrimStart());
    }

    [Theory]
    [InlineData(MermaidDiagramType.Flowchart)]
    [InlineData(MermaidDiagramType.Sequence)]
    [InlineData(MermaidDiagramType.Class)]
    [InlineData(MermaidDiagramType.Er)]
    [InlineData(MermaidDiagramType.State)]
    [InlineData(MermaidDiagramType.Gantt)]
    [InlineData(MermaidDiagramType.Mindmap)]
    [InlineData(MermaidDiagramType.Pie)]
    public void GetTemplate_ContainsPortugueseComment(MermaidDiagramType type)
    {
        var svc = new MermaidTemplateService();
        var text = svc.GetTemplate(type);
        Assert.Contains("%%", text);
    }

    [Fact]
    public void GetTemplate_SecondCall_ReturnsSameInstance()
    {
        var svc = new MermaidTemplateService();
        var a = svc.GetTemplate(MermaidDiagramType.Flowchart);
        var b = svc.GetTemplate(MermaidDiagramType.Flowchart);
        Assert.Same(a, b);
    }
}
```

- [ ] **Step 2: Rodar — deve falhar (classe ainda não existe)**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~MermaidTemplateServiceTests"
```
Expected: build error (`MermaidTemplateService` does not exist).

- [ ] **Step 3: Implementar `MermaidTemplateService`**

`src/MeuMarkdown/Services/MermaidTemplateService.cs`:
```csharp
using System.Collections.Concurrent;
using System.IO;
using System.Reflection;
using MeuMarkdown.Models.Mermaid;

namespace MeuMarkdown.Services;

public class MermaidTemplateService
{
    private readonly ConcurrentDictionary<MermaidDiagramType, string> _cache = new();

    public string GetTemplate(MermaidDiagramType type)
    {
        return _cache.GetOrAdd(type, Load);
    }

    private static string Load(MermaidDiagramType type)
    {
        var fileName = type switch
        {
            MermaidDiagramType.Flowchart => "flowchart-td.txt",
            MermaidDiagramType.Sequence => "sequence.txt",
            MermaidDiagramType.Class => "class.txt",
            MermaidDiagramType.Er => "er.txt",
            MermaidDiagramType.State => "state.txt",
            MermaidDiagramType.Gantt => "gantt.txt",
            MermaidDiagramType.Mindmap => "mindmap.txt",
            MermaidDiagramType.Pie => "pie.txt",
            _ => throw new ArgumentOutOfRangeException(nameof(type)),
        };

        var resourceName = $"MeuMarkdown.Resources.MermaidTemplates.{fileName}";
        var asm = typeof(MermaidTemplateService).Assembly;
        using var stream = asm.GetManifestResourceStream(resourceName)
            ?? throw new InvalidOperationException($"Template não encontrado: {resourceName}");
        using var reader = new StreamReader(stream);
        return reader.ReadToEnd().Trim();
    }
}
```

- [ ] **Step 4: Rodar testes — devem passar**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~MermaidTemplateServiceTests"
```
Expected: Passed.

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/Services/MermaidTemplateService.cs tests/MeuMarkdown.Tests/Services/MermaidTemplateServiceTests.cs
git commit -m "feat(mermaid): MermaidTemplateService carrega templates de Resources com cache"
```

---

## Task 3 — Enums e modelos de Flowchart

**Files:**
- Create: `src/MeuMarkdown/Models/Mermaid/FlowDirection.cs`
- Create: `src/MeuMarkdown/Models/Mermaid/FlowNodeShape.cs`
- Create: `src/MeuMarkdown/Models/Mermaid/FlowArrowType.cs`
- Create: `src/MeuMarkdown/Models/Mermaid/FlowNode.cs`
- Create: `src/MeuMarkdown/Models/Mermaid/FlowEdge.cs`

- [ ] **Step 1: Criar enums**

`FlowDirection.cs`:
```csharp
namespace MeuMarkdown.Models.Mermaid;
public enum FlowDirection { TD, LR, BT, RL }
```

`FlowNodeShape.cs`:
```csharp
namespace MeuMarkdown.Models.Mermaid;
public enum FlowNodeShape { Rectangle, Rounded, Diamond, Circle, Stadium }
```

`FlowArrowType.cs`:
```csharp
namespace MeuMarkdown.Models.Mermaid;
public enum FlowArrowType { Solid, SolidLabeled, Dotted, Thick }
```

- [ ] **Step 2: Criar `FlowNode`**

`FlowNode.cs`:
```csharp
using CommunityToolkit.Mvvm.ComponentModel;

namespace MeuMarkdown.Models.Mermaid;

public partial class FlowNode : ObservableObject
{
    [ObservableProperty] private string _id = "";
    [ObservableProperty] private string _label = "";
    [ObservableProperty] private FlowNodeShape _shape = FlowNodeShape.Rectangle;
}
```

- [ ] **Step 3: Criar `FlowEdge`**

`FlowEdge.cs`:
```csharp
using CommunityToolkit.Mvvm.ComponentModel;

namespace MeuMarkdown.Models.Mermaid;

public partial class FlowEdge : ObservableObject
{
    [ObservableProperty] private string _fromId = "";
    [ObservableProperty] private string _toId = "";
    [ObservableProperty] private FlowArrowType _arrow = FlowArrowType.Solid;
    [ObservableProperty] private string _label = "";
}
```

- [ ] **Step 4: Build sanity**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded.

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/Models/Mermaid/
git commit -m "feat(mermaid): enums e ObservableObjects para FlowNode/FlowEdge"
```

---

## Task 4 — `FlowchartModel.ToMermaid()` base + shapes (TDD)

**Files:**
- Create: `src/MeuMarkdown/Models/Mermaid/FlowchartModel.cs`
- Test: `tests/MeuMarkdown.Tests/Models/Mermaid/FlowchartModelTests.cs`

- [ ] **Step 1: Escrever testes base + shapes (falham)**

`tests/MeuMarkdown.Tests/Models/Mermaid/FlowchartModelTests.cs`:
```csharp
using MeuMarkdown.Models.Mermaid;

namespace MeuMarkdown.Tests.Models.Mermaid;

public class FlowchartModelTests
{
    [Fact]
    public void ToMermaid_EmptyModel_StartsWithGraphTD()
    {
        var model = new FlowchartModel();
        var output = model.ToMermaid();
        Assert.StartsWith("graph TD", output);
    }

    [Theory]
    [InlineData(FlowDirection.TD, "graph TD")]
    [InlineData(FlowDirection.LR, "graph LR")]
    [InlineData(FlowDirection.BT, "graph BT")]
    [InlineData(FlowDirection.RL, "graph RL")]
    public void ToMermaid_Direction_EmitsCorrectHeader(FlowDirection dir, string expected)
    {
        var model = new FlowchartModel { Direction = dir };
        Assert.StartsWith(expected, model.ToMermaid());
    }

    [Theory]
    [InlineData(FlowNodeShape.Rectangle, "n1[Olá]")]
    [InlineData(FlowNodeShape.Rounded, "n1(Olá)")]
    [InlineData(FlowNodeShape.Diamond, "n1{Olá}")]
    [InlineData(FlowNodeShape.Circle, "n1((Olá))")]
    [InlineData(FlowNodeShape.Stadium, "n1([Olá])")]
    public void ToMermaid_Shape_EmitsCorrectSyntax(FlowNodeShape shape, string expectedFragment)
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "Olá", Shape = shape });
        Assert.Contains(expectedFragment, model.ToMermaid());
    }
}
```

- [ ] **Step 2: Rodar — falha (FlowchartModel não existe)**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~FlowchartModelTests"
```
Expected: build error.

- [ ] **Step 3: Implementar `FlowchartModel` mínimo**

`src/MeuMarkdown/Models/Mermaid/FlowchartModel.cs`:
```csharp
using System.Collections.ObjectModel;
using System.Text;
using CommunityToolkit.Mvvm.ComponentModel;

namespace MeuMarkdown.Models.Mermaid;

public partial class FlowchartModel : ObservableObject
{
    [ObservableProperty] private FlowDirection _direction = FlowDirection.TD;

    public ObservableCollection<FlowNode> Nodes { get; } = new();
    public ObservableCollection<FlowEdge> Edges { get; } = new();

    public string ToMermaid()
    {
        var sb = new StringBuilder();
        sb.Append("graph ").Append(Direction.ToString()).AppendLine();

        foreach (var node in Nodes)
        {
            var (open, close) = ShapeBrackets(node.Shape);
            sb.Append("    ").Append(node.Id).Append(open).Append(node.Label).Append(close).AppendLine();
        }

        return sb.ToString().TrimEnd();
    }

    private static (string open, string close) ShapeBrackets(FlowNodeShape shape) => shape switch
    {
        FlowNodeShape.Rectangle => ("[", "]"),
        FlowNodeShape.Rounded   => ("(", ")"),
        FlowNodeShape.Diamond   => ("{", "}"),
        FlowNodeShape.Circle    => ("((", "))"),
        FlowNodeShape.Stadium   => ("([", "])"),
        _ => ("[", "]"),
    };
}
```

- [ ] **Step 4: Rodar testes — devem passar**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~FlowchartModelTests"
```
Expected: Passed.

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/Models/Mermaid/FlowchartModel.cs tests/MeuMarkdown.Tests/Models/Mermaid/FlowchartModelTests.cs
git commit -m "feat(mermaid): FlowchartModel.ToMermaid() com direction e shapes"
```

---

## Task 5 — Flowchart: arrows, labels e escape (TDD)

**Files:**
- Modify: `src/MeuMarkdown/Models/Mermaid/FlowchartModel.cs`
- Modify: `tests/MeuMarkdown.Tests/Models/Mermaid/FlowchartModelTests.cs`

- [ ] **Step 1: Adicionar testes (falham)**

Acrescentar ao final de `FlowchartModelTests.cs` (antes da chave fechando a classe):
```csharp
    [Theory]
    [InlineData(FlowArrowType.Solid, "n1 --> n2")]
    [InlineData(FlowArrowType.Dotted, "n1 -.-> n2")]
    [InlineData(FlowArrowType.Thick, "n1 ==> n2")]
    public void ToMermaid_Arrow_EmitsCorrectSyntax(FlowArrowType arrow, string expected)
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "A" });
        model.Nodes.Add(new FlowNode { Id = "n2", Label = "B" });
        model.Edges.Add(new FlowEdge { FromId = "n1", ToId = "n2", Arrow = arrow });
        Assert.Contains(expected, model.ToMermaid());
    }

    [Fact]
    public void ToMermaid_SolidLabeledArrow_EmitsLabelBetweenPipes()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "A" });
        model.Nodes.Add(new FlowNode { Id = "n2", Label = "B" });
        model.Edges.Add(new FlowEdge { FromId = "n1", ToId = "n2", Arrow = FlowArrowType.SolidLabeled, Label = "sim" });
        Assert.Contains("n1 -->|sim| n2", model.ToMermaid());
    }

    [Fact]
    public void ToMermaid_SolidArrow_IgnoresLabelEvenIfPresent()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "A" });
        model.Nodes.Add(new FlowNode { Id = "n2", Label = "B" });
        model.Edges.Add(new FlowEdge { FromId = "n1", ToId = "n2", Arrow = FlowArrowType.Solid, Label = "ignorado" });
        Assert.Contains("n1 --> n2", model.ToMermaid());
        Assert.DoesNotContain("|ignorado|", model.ToMermaid());
    }

    [Fact]
    public void ToMermaid_LabelWithQuotes_EscapesToSingleQuotes()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "Tem \"aspas\"" });
        var output = model.ToMermaid();
        Assert.Contains("Tem 'aspas'", output);
        Assert.DoesNotContain("\"aspas\"", output);
    }

    [Fact]
    public void ToMermaid_LabelWithNewline_EmitsBrTag()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "Linha 1\nLinha 2" });
        Assert.Contains("Linha 1<br/>Linha 2", model.ToMermaid());
    }

    [Fact]
    public void ToMermaid_LabelWithBrackets_WrapsInDoubleQuotes()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "Texto (a) [b]", Shape = FlowNodeShape.Rectangle });
        Assert.Contains("n1[\"Texto (a) [b]\"]", model.ToMermaid());
    }
```

- [ ] **Step 2: Rodar — falham**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~FlowchartModelTests"
```
Expected: vários FAIL.

- [ ] **Step 3: Implementar arrows e escape no `FlowchartModel`**

Substituir o corpo de `FlowchartModel.cs` por:
```csharp
using System.Collections.ObjectModel;
using System.Text;
using CommunityToolkit.Mvvm.ComponentModel;

namespace MeuMarkdown.Models.Mermaid;

public partial class FlowchartModel : ObservableObject
{
    [ObservableProperty] private FlowDirection _direction = FlowDirection.TD;

    public ObservableCollection<FlowNode> Nodes { get; } = new();
    public ObservableCollection<FlowEdge> Edges { get; } = new();

    public string ToMermaid()
    {
        var sb = new StringBuilder();
        sb.Append("graph ").Append(Direction.ToString()).AppendLine();

        foreach (var node in Nodes)
            sb.Append("    ").Append(EmitNode(node)).AppendLine();

        foreach (var edge in Edges)
            sb.Append("    ").Append(EmitEdge(edge)).AppendLine();

        return sb.ToString().TrimEnd();
    }

    private static string EmitNode(FlowNode node)
    {
        var (open, close) = ShapeBrackets(node.Shape);
        var label = EscapeLabel(node.Label);
        if (NeedsQuoting(node.Label))
            return $"{node.Id}{open}\"{label}\"{close}";
        return $"{node.Id}{open}{label}{close}";
    }

    private static string EmitEdge(FlowEdge edge)
    {
        return edge.Arrow switch
        {
            FlowArrowType.Solid        => $"{edge.FromId} --> {edge.ToId}",
            FlowArrowType.SolidLabeled => $"{edge.FromId} -->|{EscapeLabel(edge.Label)}| {edge.ToId}",
            FlowArrowType.Dotted       => $"{edge.FromId} -.-> {edge.ToId}",
            FlowArrowType.Thick        => $"{edge.FromId} ==> {edge.ToId}",
            _                          => $"{edge.FromId} --> {edge.ToId}",
        };
    }

    private static string EscapeLabel(string label)
    {
        return label
            .Replace("\"", "'")
            .Replace("\r\n", "<br/>")
            .Replace("\n", "<br/>")
            .Replace("\r", "<br/>");
    }

    private static bool NeedsQuoting(string label)
    {
        // Brackets dentro de label de Flowchart precisam ser envolvidos por aspas duplas.
        return label.IndexOfAny(new[] { '[', ']', '(', ')', '{', '}' }) >= 0;
    }

    private static (string open, string close) ShapeBrackets(FlowNodeShape shape) => shape switch
    {
        FlowNodeShape.Rectangle => ("[", "]"),
        FlowNodeShape.Rounded   => ("(", ")"),
        FlowNodeShape.Diamond   => ("{", "}"),
        FlowNodeShape.Circle    => ("((", "))"),
        FlowNodeShape.Stadium   => ("([", "])"),
        _ => ("[", "]"),
    };
}
```

- [ ] **Step 4: Rodar — todos passam**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~FlowchartModelTests"
```
Expected: Passed.

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/Models/Mermaid/FlowchartModel.cs tests/MeuMarkdown.Tests/Models/Mermaid/FlowchartModelTests.cs
git commit -m "feat(mermaid): arrows, labels e escape no FlowchartModel"
```

---

## Task 6 — Flowchart: validação (orphan / duplicate / invalid id) (TDD)

**Files:**
- Modify: `src/MeuMarkdown/Models/Mermaid/FlowchartModel.cs`
- Modify: `tests/MeuMarkdown.Tests/Models/Mermaid/FlowchartModelTests.cs`

- [ ] **Step 1: Acrescentar testes (falham)**

```csharp
    [Fact]
    public void ToMermaid_EdgeWithOrphanFromId_IsOmitted()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "A" });
        model.Edges.Add(new FlowEdge { FromId = "ghost", ToId = "n1", Arrow = FlowArrowType.Solid });
        var output = model.ToMermaid();
        Assert.DoesNotContain("ghost", output);
    }

    [Fact]
    public void ToMermaid_EdgeWithOrphanToId_IsOmitted()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "A" });
        model.Edges.Add(new FlowEdge { FromId = "n1", ToId = "ghost", Arrow = FlowArrowType.Solid });
        Assert.DoesNotContain("ghost", model.ToMermaid());
    }

    [Fact]
    public void ToMermaid_DuplicateNodeId_EmitsOnlyFirst()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "Primeiro" });
        model.Nodes.Add(new FlowNode { Id = "n1", Label = "Segundo" });
        var output = model.ToMermaid();
        Assert.Contains("Primeiro", output);
        Assert.DoesNotContain("Segundo", output);
    }

    [Fact]
    public void ToMermaid_EmptyOrInvalidId_UsesTempId()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "", Label = "Sem ID" });
        var output = model.ToMermaid();
        // Ao menos o label deve estar lá, e algum ID temp começando com "n_" deve ter sido emitido.
        Assert.Contains("Sem ID", output);
        Assert.Matches(@"n_[a-z0-9]+\[Sem ID\]", output);
    }

    [Fact]
    public void ToMermaid_InvalidIdWithSpaces_UsesTempId()
    {
        var model = new FlowchartModel();
        model.Nodes.Add(new FlowNode { Id = "id com espaço", Label = "X" });
        var output = model.ToMermaid();
        Assert.DoesNotContain("id com espaço", output);
        Assert.Contains("[X]", output);
    }
```

- [ ] **Step 2: Rodar — falham**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~FlowchartModelTests"
```
Expected: FAIL nas novas.

- [ ] **Step 3: Implementar validação em `FlowchartModel`**

Substituir o corpo de `FlowchartModel.cs` por (mantém o anterior e adiciona validação):
```csharp
using System.Collections.ObjectModel;
using System.Text;
using System.Text.RegularExpressions;
using CommunityToolkit.Mvvm.ComponentModel;

namespace MeuMarkdown.Models.Mermaid;

public partial class FlowchartModel : ObservableObject
{
    [ObservableProperty] private FlowDirection _direction = FlowDirection.TD;

    public ObservableCollection<FlowNode> Nodes { get; } = new();
    public ObservableCollection<FlowEdge> Edges { get; } = new();

    private static readonly Regex ValidIdRegex = new(@"^[A-Za-z_][A-Za-z0-9_]*$", RegexOptions.Compiled);

    public string ToMermaid()
    {
        var sb = new StringBuilder();
        sb.Append("graph ").Append(Direction.ToString()).AppendLine();

        var emittedIds = new HashSet<string>(StringComparer.Ordinal);
        var idMap = new Dictionary<FlowNode, string>();

        foreach (var node in Nodes)
        {
            var id = ResolveId(node, emittedIds);
            if (id == null) continue; // duplicado
            idMap[node] = id;
            emittedIds.Add(id);
            sb.Append("    ").Append(EmitNode(id, node)).AppendLine();
        }

        // IDs de nós válidos pra checar arestas órfãs.
        var validIds = new HashSet<string>(idMap.Values, StringComparer.Ordinal);

        foreach (var edge in Edges)
        {
            if (!validIds.Contains(edge.FromId) || !validIds.Contains(edge.ToId)) continue;
            sb.Append("    ").Append(EmitEdge(edge)).AppendLine();
        }

        return sb.ToString().TrimEnd();
    }

    private static string? ResolveId(FlowNode node, HashSet<string> alreadyEmitted)
    {
        var raw = node.Id ?? "";
        var id = ValidIdRegex.IsMatch(raw) ? raw : GenerateTempId(node);

        if (alreadyEmitted.Contains(id))
        {
            // duplicado — pulamos o nó (primeiro vence)
            return null;
        }
        return id;
    }

    private static string GenerateTempId(FlowNode node)
    {
        var hash = (node.GetHashCode() & 0x7fffffff).ToString("x8");
        return "n_" + hash;
    }

    private static string EmitNode(string id, FlowNode node)
    {
        var (open, close) = ShapeBrackets(node.Shape);
        var label = EscapeLabel(node.Label);
        if (NeedsQuoting(node.Label))
            return $"{id}{open}\"{label}\"{close}";
        return $"{id}{open}{label}{close}";
    }

    private static string EmitEdge(FlowEdge edge)
    {
        return edge.Arrow switch
        {
            FlowArrowType.Solid        => $"{edge.FromId} --> {edge.ToId}",
            FlowArrowType.SolidLabeled => $"{edge.FromId} -->|{EscapeLabel(edge.Label)}| {edge.ToId}",
            FlowArrowType.Dotted       => $"{edge.FromId} -.-> {edge.ToId}",
            FlowArrowType.Thick        => $"{edge.FromId} ==> {edge.ToId}",
            _                          => $"{edge.FromId} --> {edge.ToId}",
        };
    }

    private static string EscapeLabel(string label)
    {
        return label
            .Replace("\"", "'")
            .Replace("\r\n", "<br/>")
            .Replace("\n", "<br/>")
            .Replace("\r", "<br/>");
    }

    private static bool NeedsQuoting(string label)
    {
        return label.IndexOfAny(new[] { '[', ']', '(', ')', '{', '}' }) >= 0;
    }

    private static (string open, string close) ShapeBrackets(FlowNodeShape shape) => shape switch
    {
        FlowNodeShape.Rectangle => ("[", "]"),
        FlowNodeShape.Rounded   => ("(", ")"),
        FlowNodeShape.Diamond   => ("{", "}"),
        FlowNodeShape.Circle    => ("((", "))"),
        FlowNodeShape.Stadium   => ("([", "])"),
        _ => ("[", "]"),
    };
}
```

> Nota sobre `EmitEdge`: usa `edge.FromId` / `edge.ToId` (raw, validados acima). Não traduz IDs temp. Se um nó com ID inválido precisar de aresta, fica responsabilidade do usuário corrigir o ID. Isso é simples e suficiente para o v1.

- [ ] **Step 4: Rodar — todos passam**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~FlowchartModelTests"
```
Expected: Passed.

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/Models/Mermaid/FlowchartModel.cs tests/MeuMarkdown.Tests/Models/Mermaid/FlowchartModelTests.cs
git commit -m "feat(mermaid): validação leve do FlowchartModel (orphan/duplicate/invalid id)"
```

---

## Task 7 — Modelos de Sequence

**Files:**
- Create: `src/MeuMarkdown/Models/Mermaid/SequenceArrowType.cs`
- Create: `src/MeuMarkdown/Models/Mermaid/Actor.cs`
- Create: `src/MeuMarkdown/Models/Mermaid/Message.cs`

- [ ] **Step 1: Criar `SequenceArrowType`**

```csharp
namespace MeuMarkdown.Models.Mermaid;
public enum SequenceArrowType { Sync, Async, Reply }
```

- [ ] **Step 2: Criar `Actor`**

```csharp
using CommunityToolkit.Mvvm.ComponentModel;

namespace MeuMarkdown.Models.Mermaid;

public partial class Actor : ObservableObject
{
    [ObservableProperty] private string _name = "";
    [ObservableProperty] private string _alias = "";
    [ObservableProperty] private bool _isActor;
}
```

- [ ] **Step 3: Criar `Message`**

```csharp
using CommunityToolkit.Mvvm.ComponentModel;

namespace MeuMarkdown.Models.Mermaid;

public partial class Message : ObservableObject
{
    [ObservableProperty] private string _fromActor = "";
    [ObservableProperty] private string _toActor = "";
    [ObservableProperty] private SequenceArrowType _arrow = SequenceArrowType.Sync;
    [ObservableProperty] private string _label = "";
}
```

- [ ] **Step 4: Build sanity**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded.

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/Models/Mermaid/SequenceArrowType.cs src/MeuMarkdown/Models/Mermaid/Actor.cs src/MeuMarkdown/Models/Mermaid/Message.cs
git commit -m "feat(mermaid): Actor, Message e SequenceArrowType"
```

---

## Task 8 — `SequenceModel.ToMermaid()` + testes (TDD)

**Files:**
- Create: `src/MeuMarkdown/Models/Mermaid/SequenceModel.cs`
- Test: `tests/MeuMarkdown.Tests/Models/Mermaid/SequenceModelTests.cs`

- [ ] **Step 1: Escrever testes (falham)**

`tests/MeuMarkdown.Tests/Models/Mermaid/SequenceModelTests.cs`:
```csharp
using MeuMarkdown.Models.Mermaid;

namespace MeuMarkdown.Tests.Models.Mermaid;

public class SequenceModelTests
{
    [Fact]
    public void ToMermaid_EmptyModel_StartsWithSequenceDiagram()
    {
        var model = new SequenceModel();
        Assert.StartsWith("sequenceDiagram", model.ToMermaid());
    }

    [Fact]
    public void ToMermaid_ParticipantWithoutAlias_EmitsParticipantLine()
    {
        var model = new SequenceModel();
        model.Actors.Add(new Actor { Name = "Bob", IsActor = false });
        Assert.Contains("participant Bob", model.ToMermaid());
    }

    [Fact]
    public void ToMermaid_ActorWithAlias_EmitsAsClause()
    {
        var model = new SequenceModel();
        model.Actors.Add(new Actor { Name = "Cliente", Alias = "C", IsActor = true });
        Assert.Contains("actor Cliente as C", model.ToMermaid());
    }

    [Theory]
    [InlineData(SequenceArrowType.Sync, "Alice->>Bob: olá")]
    [InlineData(SequenceArrowType.Reply, "Alice-->>Bob: olá")]
    [InlineData(SequenceArrowType.Async, "Alice-)Bob: olá")]
    public void ToMermaid_Message_EmitsCorrectArrow(SequenceArrowType arrow, string expected)
    {
        var model = new SequenceModel();
        model.Actors.Add(new Actor { Name = "Alice", IsActor = true });
        model.Actors.Add(new Actor { Name = "Bob", IsActor = false });
        model.Messages.Add(new Message { FromActor = "Alice", ToActor = "Bob", Arrow = arrow, Label = "olá" });
        Assert.Contains(expected, model.ToMermaid());
    }

    [Fact]
    public void ToMermaid_MessageWithUnknownActor_IsOmitted()
    {
        var model = new SequenceModel();
        model.Actors.Add(new Actor { Name = "Alice", IsActor = true });
        model.Messages.Add(new Message { FromActor = "Alice", ToActor = "Ghost", Arrow = SequenceArrowType.Sync, Label = "olá" });
        Assert.DoesNotContain("Ghost", model.ToMermaid());
    }

    [Fact]
    public void ToMermaid_LabelWithQuotes_EscapesToSingleQuotes()
    {
        var model = new SequenceModel();
        model.Actors.Add(new Actor { Name = "Alice", IsActor = true });
        model.Actors.Add(new Actor { Name = "Bob", IsActor = false });
        model.Messages.Add(new Message { FromActor = "Alice", ToActor = "Bob", Arrow = SequenceArrowType.Sync, Label = "tem \"aspas\"" });
        var output = model.ToMermaid();
        Assert.Contains("tem 'aspas'", output);
        Assert.DoesNotContain("\"aspas\"", output);
    }

    [Fact]
    public void ToMermaid_LabelWithNewline_EmitsBrTag()
    {
        var model = new SequenceModel();
        model.Actors.Add(new Actor { Name = "Alice", IsActor = true });
        model.Actors.Add(new Actor { Name = "Bob", IsActor = false });
        model.Messages.Add(new Message { FromActor = "Alice", ToActor = "Bob", Arrow = SequenceArrowType.Sync, Label = "linha1\nlinha2" });
        Assert.Contains("linha1<br/>linha2", model.ToMermaid());
    }
}
```

- [ ] **Step 2: Rodar — falham**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~SequenceModelTests"
```
Expected: build error.

- [ ] **Step 3: Implementar `SequenceModel`**

`src/MeuMarkdown/Models/Mermaid/SequenceModel.cs`:
```csharp
using System.Collections.ObjectModel;
using System.Text;
using CommunityToolkit.Mvvm.ComponentModel;

namespace MeuMarkdown.Models.Mermaid;

public partial class SequenceModel : ObservableObject
{
    public ObservableCollection<Actor> Actors { get; } = new();
    public ObservableCollection<Message> Messages { get; } = new();

    public string ToMermaid()
    {
        var sb = new StringBuilder();
        sb.AppendLine("sequenceDiagram");

        var knownNames = new HashSet<string>(StringComparer.Ordinal);
        foreach (var actor in Actors)
        {
            if (string.IsNullOrWhiteSpace(actor.Name)) continue;
            if (!knownNames.Add(actor.Name)) continue;

            var kw = actor.IsActor ? "actor" : "participant";
            if (!string.IsNullOrWhiteSpace(actor.Alias))
                sb.Append("    ").Append(kw).Append(' ').Append(actor.Name).Append(" as ").Append(actor.Alias).AppendLine();
            else
                sb.Append("    ").Append(kw).Append(' ').Append(actor.Name).AppendLine();
        }

        foreach (var msg in Messages)
        {
            if (!knownNames.Contains(msg.FromActor) || !knownNames.Contains(msg.ToActor)) continue;
            var arrow = msg.Arrow switch
            {
                SequenceArrowType.Sync  => "->>",
                SequenceArrowType.Reply => "-->>",
                SequenceArrowType.Async => "-)",
                _                       => "->>",
            };
            sb.Append("    ")
              .Append(msg.FromActor).Append(arrow).Append(msg.ToActor)
              .Append(": ").Append(EscapeLabel(msg.Label))
              .AppendLine();
        }

        return sb.ToString().TrimEnd();
    }

    private static string EscapeLabel(string label)
    {
        return label
            .Replace("\"", "'")
            .Replace("\r\n", "<br/>")
            .Replace("\n", "<br/>")
            .Replace("\r", "<br/>");
    }
}
```

- [ ] **Step 4: Rodar — todos passam**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~SequenceModelTests"
```
Expected: Passed.

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/Models/Mermaid/SequenceModel.cs tests/MeuMarkdown.Tests/Models/Mermaid/SequenceModelTests.cs
git commit -m "feat(mermaid): SequenceModel.ToMermaid() com atores, mensagens e escape"
```

---

## Task 9 — `MarkdownInsertionService.BuildMermaidInsertion` (TDD, pure)

**Files:**
- Create: `src/MeuMarkdown/Services/MarkdownInsertionService.cs`
- Test: `tests/MeuMarkdown.Tests/Services/MarkdownInsertionServiceTests.cs`

- [ ] **Step 1: Escrever testes (falham)**

`tests/MeuMarkdown.Tests/Services/MarkdownInsertionServiceTests.cs`:
```csharp
using MeuMarkdown.Services;

namespace MeuMarkdown.Tests.Services;

public class MarkdownInsertionServiceTests
{
    [Fact]
    public void Build_OnEmptyDocument_NoPrefixNoExtraSuffix()
    {
        var (text, newOffset) = MarkdownInsertionService.BuildMermaidInsertion(
            content: "",
            caretOffset: 0,
            mermaidCode: "graph TD\n    A --> B");

        Assert.Equal("```mermaid\ngraph TD\n    A --> B\n```\n\n", text);
        Assert.Equal(text.Length, newOffset);
    }

    [Fact]
    public void Build_CursorOnEmptyLine_NoPrefix()
    {
        var doc = "Texto antes\n\n";
        var (text, _) = MarkdownInsertionService.BuildMermaidInsertion(
            content: doc,
            caretOffset: doc.Length,
            mermaidCode: "graph TD\n    A --> B");

        Assert.StartsWith("```mermaid", text);
    }

    [Fact]
    public void Build_CursorOnNonEmptyLine_PrefixesDoubleNewline()
    {
        var doc = "Texto sem quebra";
        var (text, _) = MarkdownInsertionService.BuildMermaidInsertion(
            content: doc,
            caretOffset: doc.Length,
            mermaidCode: "graph TD\n    A --> B");

        Assert.StartsWith("\n\n```mermaid", text);
    }

    [Fact]
    public void Build_AtStartOfDocument_NoPrefix()
    {
        var doc = "Texto qualquer";
        var (text, _) = MarkdownInsertionService.BuildMermaidInsertion(
            content: doc,
            caretOffset: 0,
            mermaidCode: "graph TD");

        Assert.StartsWith("```mermaid", text);
    }

    [Fact]
    public void Build_CursorMidLine_PrefixesDoubleNewline()
    {
        var doc = "abc";
        var (text, _) = MarkdownInsertionService.BuildMermaidInsertion(
            content: doc,
            caretOffset: 1,
            mermaidCode: "graph TD");

        Assert.StartsWith("\n\n```mermaid", text);
    }

    [Fact]
    public void Build_AlwaysSuffixesDoubleNewline()
    {
        var (text, _) = MarkdownInsertionService.BuildMermaidInsertion(
            content: "",
            caretOffset: 0,
            mermaidCode: "graph TD");

        Assert.EndsWith("```\n\n", text);
    }

    [Fact]
    public void Build_NewOffset_PointsToEndOfInsertion()
    {
        var doc = "Algo antes";
        var (text, newOffset) = MarkdownInsertionService.BuildMermaidInsertion(
            content: doc,
            caretOffset: doc.Length,
            mermaidCode: "graph TD");

        Assert.Equal(doc.Length + text.Length, newOffset);
    }
}
```

- [ ] **Step 2: Rodar — falham**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~MarkdownInsertionServiceTests"
```
Expected: build error.

- [ ] **Step 3: Implementar `MarkdownInsertionService`**

`src/MeuMarkdown/Services/MarkdownInsertionService.cs`:
```csharp
namespace MeuMarkdown.Services;

/// <summary>
/// Helper estático para calcular como inserir um bloco mermaid no documento,
/// dado o conteúdo atual e a posição do cursor. Pure — fácil de testar sem WPF.
/// </summary>
public static class MarkdownInsertionService
{
    public static (string text, int newCaretOffset) BuildMermaidInsertion(
        string content, int caretOffset, string mermaidCode)
    {
        var needsPrefix = NeedsLeadingBlankLine(content, caretOffset);
        var prefix = needsPrefix ? "\n\n" : "";
        var body = "```mermaid\n" + mermaidCode + "\n```\n\n";
        var text = prefix + body;
        return (text, caretOffset + text.Length);
    }

    private static bool NeedsLeadingBlankLine(string content, int caretOffset)
    {
        // No início do documento ou em linha vazia → sem prefix.
        if (caretOffset <= 0) return false;

        // Pega caractere imediatamente anterior ao caret.
        var prev = content[caretOffset - 1];
        if (prev == '\n')
        {
            // Início de linha. Se a linha anterior é vazia (\n\n) também tudo bem.
            return false;
        }
        return true;
    }
}
```

- [ ] **Step 4: Rodar — passam**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj --filter "FullyQualifiedName~MarkdownInsertionServiceTests"
```
Expected: Passed.

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/Services/MarkdownInsertionService.cs tests/MeuMarkdown.Tests/Services/MarkdownInsertionServiceTests.cs
git commit -m "feat(mermaid): MarkdownInsertionService.BuildMermaidInsertion (pure helper)"
```

---

## Task 10 — Menu "Inserir > Mermaid > Templates" + handler de inserção

**Files:**
- Modify: `src/MeuMarkdown/MainWindow.xaml`
- Modify: `src/MeuMarkdown/MainWindow.xaml.cs`

- [ ] **Step 1: Adicionar `MenuItem "_Inserir"` no XAML**

Em `src/MeuMarkdown/MainWindow.xaml`, encontrar a linha `<MenuItem Header="_Navegar">` (~linha 253) e inserir antes dela (ou depois do fechamento dela na linha ~264, antes do `<MenuItem Header="_Exibir">`) o seguinte bloco:

```xml
<MenuItem Header="_Inserir">
    <MenuItem Header="_Mermaid">
        <MenuItem Header="Fluxograma (template)" Click="OnInsertMermaidTemplate" Tag="Flowchart" />
        <MenuItem Header="Sequência (template)" Click="OnInsertMermaidTemplate" Tag="Sequence" />
        <MenuItem Header="Classe (template)" Click="OnInsertMermaidTemplate" Tag="Class" />
        <MenuItem Header="ER (template)" Click="OnInsertMermaidTemplate" Tag="Er" />
        <MenuItem Header="Estado (template)" Click="OnInsertMermaidTemplate" Tag="State" />
        <MenuItem Header="Gantt (template)" Click="OnInsertMermaidTemplate" Tag="Gantt" />
        <MenuItem Header="Mindmap (template)" Click="OnInsertMermaidTemplate" Tag="Mindmap" />
        <MenuItem Header="Pie (template)" Click="OnInsertMermaidTemplate" Tag="Pie" />
        <Separator />
        <MenuItem Header="Construtor visual…" Click="OnOpenMermaidBuilder" />
    </MenuItem>
</MenuItem>
```

- [ ] **Step 2: Adicionar handler `OnInsertMermaidTemplate` e método `InsertMermaidBlock` no code-behind**

Em `src/MeuMarkdown/MainWindow.xaml.cs`, adicionar:

1. Campo: instância do service (perto dos outros services declarados no topo da classe):
```csharp
private readonly Services.MermaidTemplateService _mermaidTemplateService = new();
```

2. Métodos (perto dos outros handlers `OnFormat...` ou `Insert...`):
```csharp
private void OnInsertMermaidTemplate(object sender, RoutedEventArgs e)
{
    if (sender is not MenuItem mi) return;
    if (mi.Tag is not string typeName) return;
    if (!Enum.TryParse<Models.Mermaid.MermaidDiagramType>(typeName, out var type)) return;

    var template = _mermaidTemplateService.GetTemplate(type);
    InsertMermaidBlock(template);
}

private void OnOpenMermaidBuilder(object sender, RoutedEventArgs e)
{
    // Implementado na Task 18.
    MessageDialog.Info(this, "Em breve", "O construtor visual chega na Task 18 deste plano.");
}

private void InsertMermaidBlock(string mermaidCode)
{
    if (textEditor == null) return;
    var content = textEditor.Document.Text ?? "";
    var caret = textEditor.CaretOffset;

    var (text, newOffset) = Services.MarkdownInsertionService
        .BuildMermaidInsertion(content, caret, mermaidCode);

    // Se há seleção, substitui — caso contrário insere no caret.
    var selection = textEditor.TextArea.Selection;
    if (!selection.IsEmpty)
    {
        var startOffset = textEditor.SelectionStart;
        var length = textEditor.SelectionLength;
        textEditor.Document.Replace(startOffset, length, text);
        textEditor.CaretOffset = startOffset + text.Length;
    }
    else
    {
        textEditor.Document.Insert(caret, text);
        textEditor.CaretOffset = newOffset;
    }
    textEditor.Focus();
}
```

- [ ] **Step 3: Build**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded.

- [ ] **Step 4: Smoke manual**

```powershell
dotnet run --project src/MeuMarkdown/MeuMarkdown.csproj
```

No app aberto:
- Abrir um arquivo `.md` (ou criar novo)
- Menu Inserir > Mermaid > Fluxograma (template)
- Confirmar: bloco ```mermaid graph TD...``` aparece no editor, preview renderiza diagrama
- Testar outros 7 itens — cada um deve inserir bloco do tipo correspondente e renderizar
- Testar "Construtor visual…" — deve mostrar diálogo "Em breve"

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/MainWindow.xaml src/MeuMarkdown/MainWindow.xaml.cs
git commit -m "feat(mermaid): menu Inserir > Mermaid com 8 templates"
```

---

## Task 11 — Página HTML do preview lite

**Files:**
- Create: `src/MeuMarkdown/Resources/mermaid-preview-lite.html`
- Modify: `src/MeuMarkdown/MeuMarkdown.csproj`

- [ ] **Step 1: Criar HTML**

`src/MeuMarkdown/Resources/mermaid-preview-lite.html`:

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8">
    <title>Mermaid Preview</title>
    <style>
        html, body {
            margin: 0;
            padding: 16px;
            background: #ffffff;
            color: #1f2328;
            font-family: 'Segoe UI', sans-serif;
            font-size: 13px;
        }
        body.dark {
            background: #1e1e1e;
            color: #d4d4d4;
        }
        #diagram { max-width: 100%; }
        #diagram svg { max-width: 100%; height: auto; }
        #error {
            color: #c0392b;
            font-family: 'Cascadia Mono', 'Consolas', monospace;
            font-size: 12px;
            white-space: pre-wrap;
            margin-top: 8px;
        }
    </style>
</head>
<body>
    <div id="diagram" class="mermaid"></div>
    <div id="error"></div>
    <script src="https://mm.local/mermaid.min.js"></script>
    <script>
        mermaid.initialize({ startOnLoad: false, theme: 'default', securityLevel: 'loose' });

        window.setBuilderTheme = function (dark) {
            document.body.classList.toggle('dark', !!dark);
            mermaid.initialize({ startOnLoad: false, theme: dark ? 'dark' : 'default', securityLevel: 'loose' });
        };

        let renderSeq = 0;
        window.render = async function (code) {
            const target = document.getElementById('diagram');
            const errBox = document.getElementById('error');
            errBox.textContent = '';

            const seq = ++renderSeq;
            try {
                const result = await mermaid.render('m' + seq, code);
                if (seq !== renderSeq) return; // descarta renders obsoletos
                target.textContent = '';
                target.insertAdjacentHTML('afterbegin', result.svg);
            } catch (err) {
                if (seq !== renderSeq) return;
                target.textContent = '';
                const msg = (err && err.message) ? err.message : String(err);
                errBox.textContent = msg;
                if (window.chrome && window.chrome.webview) {
                    window.chrome.webview.postMessage({ type: 'mermaidError', message: msg });
                }
            }
        };
    </script>
</body>
</html>
```

> Nota: `target.insertAdjacentHTML('afterbegin', svg)` é necessário porque o output de `mermaid.render` é SVG (HTML). O conteúdo vem **da biblioteca Mermaid**, não do usuário — a entrada já passou pelo parser do Mermaid antes de virar SVG. `textContent` é usado para a mensagem de erro (que pode conter input do usuário).

- [ ] **Step 2: Registrar como EmbeddedResource**

Em `src/MeuMarkdown/MeuMarkdown.csproj`, no `<ItemGroup>` de EmbeddedResource, adicionar:
```xml
<EmbeddedResource Include="Resources\mermaid-preview-lite.html" />
```

- [ ] **Step 3: Build**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded.

- [ ] **Step 4: Commit**

```powershell
git add src/MeuMarkdown/Resources/mermaid-preview-lite.html src/MeuMarkdown/MeuMarkdown.csproj
git commit -m "feat(mermaid): página HTML do preview lite (WebView2)"
```

---

## Task 12 — `MermaidPreviewLite` UserControl

**Files:**
- Create: `src/MeuMarkdown/Controls/MermaidPreviewLite.xaml`
- Create: `src/MeuMarkdown/Controls/MermaidPreviewLite.xaml.cs`

- [ ] **Step 1: Criar XAML**

`src/MeuMarkdown/Controls/MermaidPreviewLite.xaml`:
```xml
<UserControl x:Class="MeuMarkdown.Controls.MermaidPreviewLite"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <Grid Background="{DynamicResource Brush.Bg.Surface}">
        <wv2:WebView2 x:Name="webView" />
        <TextBlock x:Name="loadingText"
                   Text="Carregando preview…"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{DynamicResource Brush.Fg.Secondary}" />
    </Grid>
</UserControl>
```

- [ ] **Step 2: Criar code-behind**

`src/MeuMarkdown/Controls/MermaidPreviewLite.xaml.cs`:
```csharp
using System.IO;
using System.Reflection;
using System.Text.Json;
using System.Windows;
using System.Windows.Controls;
using Microsoft.Web.WebView2.Core;

namespace MeuMarkdown.Controls;

public partial class MermaidPreviewLite : UserControl
{
    private bool _isInitialized;
    private string? _pendingCode;
    private string? _assetsDir;
    private bool _pendingDark;

    public event Action<string>? MermaidError;

    public MermaidPreviewLite()
    {
        InitializeComponent();
        Loaded += OnLoaded;
    }

    /// <summary>
    /// Diretório onde mermaid.min.js está extraído (registrado como virtual host "mm.local").
    /// Tem que ser chamado antes do controle ser exibido.
    /// </summary>
    public void SetAssetsDirectory(string assetsDir)
    {
        _assetsDir = assetsDir;
    }

    public void SetDarkTheme(bool dark)
    {
        _pendingDark = dark;
        if (_isInitialized)
            _ = webView.CoreWebView2.ExecuteScriptAsync($"setBuilderTheme({(dark ? "true" : "false")})");
    }

    public async Task RenderAsync(string mermaidCode)
    {
        if (!_isInitialized)
        {
            _pendingCode = mermaidCode;
            return;
        }
        var encoded = JsonSerializer.Serialize(mermaidCode);
        await webView.CoreWebView2.ExecuteScriptAsync($"render({encoded})");
    }

    private async void OnLoaded(object sender, RoutedEventArgs e)
    {
        if (_isInitialized) return;
        try
        {
            var userDataFolder = Path.Combine(
                Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),
                "MeuMarkdown", "WebView2");
            var env = await CoreWebView2Environment.CreateAsync(userDataFolder: userDataFolder);
            await webView.EnsureCoreWebView2Async(env);

            webView.CoreWebView2.Settings.AreDefaultContextMenusEnabled = false;
            webView.CoreWebView2.Settings.AreDevToolsEnabled = false;

            if (!string.IsNullOrEmpty(_assetsDir))
            {
                webView.CoreWebView2.SetVirtualHostNameToFolderMapping(
                    "mm.local", _assetsDir,
                    CoreWebView2HostResourceAccessKind.Allow);
            }

            webView.CoreWebView2.WebMessageReceived += OnWebMessageReceived;

            // Carrega a página HTML do recurso embutido
            var html = LoadEmbeddedHtml();
            webView.NavigateToString(html);

            _isInitialized = true;
            loadingText.Visibility = Visibility.Collapsed;

            // Aguarda primeira navegação completar antes de renderizar / setar tema
            void OnNavCompletedOnce(object? s, CoreWebView2NavigationCompletedEventArgs ev)
            {
                webView.CoreWebView2.NavigationCompleted -= OnNavCompletedOnce;
                if (_pendingDark)
                    _ = webView.CoreWebView2.ExecuteScriptAsync("setBuilderTheme(true)");
                if (_pendingCode != null)
                {
                    _ = RenderAsync(_pendingCode);
                    _pendingCode = null;
                }
            }
            webView.CoreWebView2.NavigationCompleted += OnNavCompletedOnce;
        }
        catch (Exception ex)
        {
            loadingText.Text = $"Erro: {ex.Message}";
        }
    }

    private static string LoadEmbeddedHtml()
    {
        var asm = typeof(MermaidPreviewLite).Assembly;
        const string resourceName = "MeuMarkdown.Resources.mermaid-preview-lite.html";
        using var stream = asm.GetManifestResourceStream(resourceName)
            ?? throw new InvalidOperationException($"Recurso não encontrado: {resourceName}");
        using var reader = new StreamReader(stream);
        return reader.ReadToEnd();
    }

    private void OnWebMessageReceived(object? sender, CoreWebView2WebMessageReceivedEventArgs e)
    {
        try
        {
            var json = e.TryGetWebMessageAsString();
            if (string.IsNullOrEmpty(json)) return;
            using var doc = JsonDocument.Parse(json);
            var root = doc.RootElement;
            if (root.TryGetProperty("type", out var typeProp) && typeProp.GetString() == "mermaidError")
            {
                var msg = root.TryGetProperty("message", out var msgProp) ? msgProp.GetString() ?? "" : "";
                MermaidError?.Invoke(msg);
            }
        }
        catch { /* ignora */ }
    }
}
```

- [ ] **Step 3: Build**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded.

- [ ] **Step 4: Commit**

```powershell
git add src/MeuMarkdown/Controls/MermaidPreviewLite.xaml src/MeuMarkdown/Controls/MermaidPreviewLite.xaml.cs
git commit -m "feat(mermaid): MermaidPreviewLite UserControl com WebView2 e RenderAsync"
```

---

## Task 13 — `MermaidBuilderViewModel`: skeleton, estado inicial e debounce

**Files:**
- Create: `src/MeuMarkdown/ViewModels/MermaidBuilderViewModel.cs`

- [ ] **Step 1: Implementar ViewModel**

`src/MeuMarkdown/ViewModels/MermaidBuilderViewModel.cs`:
```csharp
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.ComponentModel;
using System.Windows.Threading;
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using MeuMarkdown.Models.Mermaid;

namespace MeuMarkdown.ViewModels;

public enum BuilderDiagramKind { Flowchart, Sequence }

public partial class MermaidBuilderViewModel : ObservableObject
{
    private readonly DispatcherTimer _debounce;

    [ObservableProperty] private BuilderDiagramKind _diagramKind = BuilderDiagramKind.Flowchart;
    [ObservableProperty] private FlowchartModel _flowchart = new();
    [ObservableProperty] private SequenceModel _sequence = new();
    [ObservableProperty] private string _generatedCode = "";
    [ObservableProperty] private string? _errorMessage;
    [ObservableProperty] private bool _isModified;

    /// <summary>
    /// Disparado quando o modelo muda e o debounce já expirou.
    /// Janela escuta isso para chamar MermaidPreviewLite.RenderAsync.
    /// </summary>
    public event Action<string>? RenderRequested;

    public MermaidBuilderViewModel()
    {
        _debounce = new DispatcherTimer { Interval = TimeSpan.FromMilliseconds(300) };
        _debounce.Tick += OnDebounceTick;

        SeedFlowchart();
        SeedSequence();
        AttachAllListeners();
        RegenerateImmediately();
    }

    private void SeedFlowchart()
    {
        Flowchart.Nodes.Add(new FlowNode { Id = "n1", Label = "Início", Shape = FlowNodeShape.Rectangle });
        Flowchart.Nodes.Add(new FlowNode { Id = "n2", Label = "Fim", Shape = FlowNodeShape.Rectangle });
        Flowchart.Edges.Add(new FlowEdge { FromId = "n1", ToId = "n2", Arrow = FlowArrowType.Solid });
    }

    private void SeedSequence()
    {
        Sequence.Actors.Add(new Actor { Name = "Alice", IsActor = true });
        Sequence.Actors.Add(new Actor { Name = "Bob", IsActor = false });
        Sequence.Messages.Add(new Message { FromActor = "Alice", ToActor = "Bob", Arrow = SequenceArrowType.Sync, Label = "Olá" });
    }

    // ─── Listeners ───

    private void AttachAllListeners()
    {
        Flowchart.PropertyChanged += OnModelChanged;
        Sequence.PropertyChanged += OnModelChanged;
        AttachCollection(Flowchart.Nodes);
        AttachCollection(Flowchart.Edges);
        AttachCollection(Sequence.Actors);
        AttachCollection(Sequence.Messages);
    }

    private void AttachCollection<T>(ObservableCollection<T> collection) where T : INotifyPropertyChanged
    {
        collection.CollectionChanged += OnCollectionChanged;
        foreach (var item in collection)
            item.PropertyChanged += OnItemChanged;
    }

    private void OnCollectionChanged(object? sender, NotifyCollectionChangedEventArgs e)
    {
        if (e.NewItems != null)
            foreach (INotifyPropertyChanged item in e.NewItems)
                item.PropertyChanged += OnItemChanged;
        if (e.OldItems != null)
            foreach (INotifyPropertyChanged item in e.OldItems)
                item.PropertyChanged -= OnItemChanged;
        OnModelChanged(sender, new PropertyChangedEventArgs("Collection"));
    }

    private void OnItemChanged(object? sender, PropertyChangedEventArgs e) => OnModelChanged(sender, e);

    private void OnModelChanged(object? sender, PropertyChangedEventArgs e)
    {
        IsModified = true;
        _debounce.Stop();
        _debounce.Start();
    }

    private void OnDebounceTick(object? sender, EventArgs e)
    {
        _debounce.Stop();
        RegenerateImmediately();
    }

    public void RegenerateImmediately()
    {
        GeneratedCode = DiagramKind switch
        {
            BuilderDiagramKind.Flowchart => Flowchart.ToMermaid(),
            BuilderDiagramKind.Sequence => Sequence.ToMermaid(),
            _ => "",
        };
        RenderRequested?.Invoke(GeneratedCode);
    }

    partial void OnDiagramKindChanged(BuilderDiagramKind value)
    {
        RegenerateImmediately();
    }

    /// <summary>
    /// Chamado pelo MermaidPreviewLite quando recebe erro de render.
    /// </summary>
    public void SetRenderError(string? message)
    {
        ErrorMessage = string.IsNullOrEmpty(message) ? null : "⚠ " + message;
    }
}
```

- [ ] **Step 2: Build**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded.

- [ ] **Step 3: Commit**

```powershell
git add src/MeuMarkdown/ViewModels/MermaidBuilderViewModel.cs
git commit -m "feat(mermaid): MermaidBuilderViewModel com debounce e estado inicial"
```

---

## Task 14 — `FlowchartBuilderPanel` (UserControl)

**Files:**
- Create: `src/MeuMarkdown/Controls/MermaidBuilderPanels/FlowchartBuilderPanel.xaml`
- Create: `src/MeuMarkdown/Controls/MermaidBuilderPanels/FlowchartBuilderPanel.xaml.cs`

- [ ] **Step 1: Criar XAML**

`src/MeuMarkdown/Controls/MermaidBuilderPanels/FlowchartBuilderPanel.xaml`:
```xml
<UserControl x:Class="MeuMarkdown.Controls.MermaidBuilderPanels.FlowchartBuilderPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:mm="clr-namespace:MeuMarkdown.Models.Mermaid"
             Background="{DynamicResource Brush.Bg.Surface}"
             Foreground="{DynamicResource Brush.Fg.Primary}">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="ShapesEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="mm:FlowNodeShape" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="ArrowsEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="mm:FlowArrowType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="12">

            <TextBlock Text="Direção" FontWeight="SemiBold" Margin="0,0,0,4" />
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <RadioButton Content="TD" GroupName="dir" IsChecked="{Binding Flowchart.Direction, Converter={StaticResource EnumToBool}, ConverterParameter=TD}" Margin="0,0,8,0" />
                <RadioButton Content="LR" GroupName="dir" IsChecked="{Binding Flowchart.Direction, Converter={StaticResource EnumToBool}, ConverterParameter=LR}" Margin="0,0,8,0" />
                <RadioButton Content="BT" GroupName="dir" IsChecked="{Binding Flowchart.Direction, Converter={StaticResource EnumToBool}, ConverterParameter=BT}" Margin="0,0,8,0" />
                <RadioButton Content="RL" GroupName="dir" IsChecked="{Binding Flowchart.Direction, Converter={StaticResource EnumToBool}, ConverterParameter=RL}" />
            </StackPanel>

            <!-- Nós -->
            <GroupBox Header="Nós" Margin="0,0,0,12">
                <StackPanel Margin="6">
                    <ListBox x:Name="NodesList"
                             ItemsSource="{Binding Flowchart.Nodes}"
                             Height="120"
                             SelectionChanged="NodesList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <Run Text="{Binding Id}" FontWeight="SemiBold" />
                                    <Run Text=" — " />
                                    <Run Text="{Binding Label}" />
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <Button Content="+ Adicionar" Click="OnAddNode" Margin="0,0,6,0" />
                        <Button Content="Remover" Click="OnRemoveNode" Margin="0,0,6,0" />
                        <Button Content="↑" Click="OnMoveNodeUp" Margin="0,0,6,0" Width="32" />
                        <Button Content="↓" Click="OnMoveNodeDown" Width="32" />
                    </StackPanel>

                    <StackPanel x:Name="NodeEditor" Margin="0,12,0,0"
                                IsEnabled="{Binding ElementName=NodesList, Path=SelectedItem, Converter={StaticResource NullToFalseConverter}}">
                        <TextBlock Text="ID" Margin="0,0,0,2" />
                        <TextBox Text="{Binding ElementName=NodesList, Path=SelectedItem.Id, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                        <TextBlock Text="Label" Margin="0,0,0,2" />
                        <TextBox Text="{Binding ElementName=NodesList, Path=SelectedItem.Label, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                        <TextBlock Text="Formato" Margin="0,0,0,2" />
                        <ComboBox ItemsSource="{Binding Source={StaticResource ShapesEnum}}"
                                  SelectedItem="{Binding ElementName=NodesList, Path=SelectedItem.Shape, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Arestas -->
            <GroupBox Header="Arestas">
                <StackPanel Margin="6">
                    <ListBox x:Name="EdgesList"
                             ItemsSource="{Binding Flowchart.Edges}"
                             Height="100"
                             SelectionChanged="EdgesList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <Run Text="{Binding FromId}" />
                                    <Run Text=" → " />
                                    <Run Text="{Binding ToId}" />
                                    <Run Text=" " />
                                    <Run Text="{Binding Label}" FontStyle="Italic" Foreground="{DynamicResource Brush.Fg.Secondary}" />
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <Button Content="+ Adicionar" Click="OnAddEdge" Margin="0,0,6,0" />
                        <Button Content="Remover" Click="OnRemoveEdge" />
                    </StackPanel>

                    <StackPanel x:Name="EdgeEditor" Margin="0,12,0,0"
                                IsEnabled="{Binding ElementName=EdgesList, Path=SelectedItem, Converter={StaticResource NullToFalseConverter}}">
                        <TextBlock Text="De" Margin="0,0,0,2" />
                        <ComboBox ItemsSource="{Binding Flowchart.Nodes}"
                                  DisplayMemberPath="Id"
                                  SelectedValuePath="Id"
                                  SelectedValue="{Binding ElementName=EdgesList, Path=SelectedItem.FromId, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,0,0,6" />
                        <TextBlock Text="Para" Margin="0,0,0,2" />
                        <ComboBox ItemsSource="{Binding Flowchart.Nodes}"
                                  DisplayMemberPath="Id"
                                  SelectedValuePath="Id"
                                  SelectedValue="{Binding ElementName=EdgesList, Path=SelectedItem.ToId, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,0,0,6" />
                        <TextBlock Text="Tipo" Margin="0,0,0,2" />
                        <ComboBox ItemsSource="{Binding Source={StaticResource ArrowsEnum}}"
                                  SelectedItem="{Binding ElementName=EdgesList, Path=SelectedItem.Arrow, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,0,0,6" />
                        <TextBlock Text="Label (apenas para Sólida c/ label)" Margin="0,0,0,2" />
                        <TextBox Text="{Binding ElementName=EdgesList, Path=SelectedItem.Label, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
```

> Nota sobre conversores: o XAML usa `EnumToBool` e `NullToFalseConverter`. Ambos são criados e registrados em `App.xaml` na Task 16 — ficam visíveis para todos os UserControls em runtime. Build do projeto passa nesta task; runtime correto só após Task 16.

- [ ] **Step 2: Criar code-behind com handlers de Add/Remove/Move**

`src/MeuMarkdown/Controls/MermaidBuilderPanels/FlowchartBuilderPanel.xaml.cs`:
```csharp
using System.Windows;
using System.Windows.Controls;
using MeuMarkdown.Models.Mermaid;
using MeuMarkdown.ViewModels;

namespace MeuMarkdown.Controls.MermaidBuilderPanels;

public partial class FlowchartBuilderPanel : UserControl
{
    public FlowchartBuilderPanel()
    {
        InitializeComponent();
    }

    private MermaidBuilderViewModel? Vm => DataContext as MermaidBuilderViewModel;

    private void OnAddNode(object sender, RoutedEventArgs e)
    {
        if (Vm == null) return;
        var existingIds = Vm.Flowchart.Nodes.Select(n => n.Id).ToHashSet();
        var i = 1;
        while (existingIds.Contains($"n{i}")) i++;
        var node = new FlowNode { Id = $"n{i}", Label = "Novo nó", Shape = FlowNodeShape.Rectangle };
        Vm.Flowchart.Nodes.Add(node);
        NodesList.SelectedItem = node;
    }

    private void OnRemoveNode(object sender, RoutedEventArgs e)
    {
        if (Vm == null) return;
        if (NodesList.SelectedItem is not FlowNode node) return;
        Vm.Flowchart.Nodes.Remove(node);
    }

    private void OnMoveNodeUp(object sender, RoutedEventArgs e) => Move(NodesList, Vm?.Flowchart.Nodes, -1);
    private void OnMoveNodeDown(object sender, RoutedEventArgs e) => Move(NodesList, Vm?.Flowchart.Nodes, +1);

    private void OnAddEdge(object sender, RoutedEventArgs e)
    {
        if (Vm == null) return;
        if (Vm.Flowchart.Nodes.Count < 2) return;
        var edge = new FlowEdge
        {
            FromId = Vm.Flowchart.Nodes[0].Id,
            ToId = Vm.Flowchart.Nodes[1].Id,
            Arrow = FlowArrowType.Solid,
        };
        Vm.Flowchart.Edges.Add(edge);
        EdgesList.SelectedItem = edge;
    }

    private void OnRemoveEdge(object sender, RoutedEventArgs e)
    {
        if (Vm == null) return;
        if (EdgesList.SelectedItem is not FlowEdge edge) return;
        Vm.Flowchart.Edges.Remove(edge);
    }

    private static void Move<T>(ListBox list, System.Collections.ObjectModel.ObservableCollection<T>? coll, int delta)
    {
        if (coll == null) return;
        if (list.SelectedItem is not T item) return;
        var idx = coll.IndexOf(item);
        var newIdx = idx + delta;
        if (newIdx < 0 || newIdx >= coll.Count) return;
        coll.Move(idx, newIdx);
        list.SelectedItem = item;
    }

    private void NodesList_SelectionChanged(object sender, SelectionChangedEventArgs e) { }
    private void EdgesList_SelectionChanged(object sender, SelectionChangedEventArgs e) { }
}
```

- [ ] **Step 3: Build (vai falhar — conversores ausentes; ok por ora)**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: provavelmente FALHA com referência a `EnumToBool`/`NullToFalseConverter` não encontradas. Isso será resolvido na Task 16. Se a falha for só XAML em runtime, build pode passar. Se passar, perfeito; se não, prossiga: a Task 16 cria os conversores.

- [ ] **Step 4: Commit**

```powershell
git add src/MeuMarkdown/Controls/MermaidBuilderPanels/
git commit -m "feat(mermaid): FlowchartBuilderPanel UserControl com formulário de nós e arestas"
```

---

## Task 15 — `SequenceBuilderPanel` (UserControl)

**Files:**
- Create: `src/MeuMarkdown/Controls/MermaidBuilderPanels/SequenceBuilderPanel.xaml`
- Create: `src/MeuMarkdown/Controls/MermaidBuilderPanels/SequenceBuilderPanel.xaml.cs`

- [ ] **Step 1: Criar XAML**

`src/MeuMarkdown/Controls/MermaidBuilderPanels/SequenceBuilderPanel.xaml`:
```xml
<UserControl x:Class="MeuMarkdown.Controls.MermaidBuilderPanels.SequenceBuilderPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:mm="clr-namespace:MeuMarkdown.Models.Mermaid"
             Background="{DynamicResource Brush.Bg.Surface}"
             Foreground="{DynamicResource Brush.Fg.Primary}">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="SeqArrowsEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="mm:SequenceArrowType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="12">

            <!-- Atores -->
            <GroupBox Header="Atores" Margin="0,0,0,12">
                <StackPanel Margin="6">
                    <ListBox x:Name="ActorsList"
                             ItemsSource="{Binding Sequence.Actors}"
                             Height="100">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <Run Text="{Binding Name}" FontWeight="SemiBold" />
                                    <Run Text=" " />
                                    <Run Text="{Binding Alias}" Foreground="{DynamicResource Brush.Fg.Secondary}" />
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <Button Content="+ Adicionar" Click="OnAddActor" Margin="0,0,6,0" />
                        <Button Content="Remover" Click="OnRemoveActor" Margin="0,0,6,0" />
                        <Button Content="↑" Click="OnMoveActorUp" Margin="0,0,6,0" Width="32" />
                        <Button Content="↓" Click="OnMoveActorDown" Width="32" />
                    </StackPanel>

                    <StackPanel x:Name="ActorEditor" Margin="0,12,0,0"
                                IsEnabled="{Binding ElementName=ActorsList, Path=SelectedItem, Converter={StaticResource NullToFalseConverter}}">
                        <TextBlock Text="Nome" Margin="0,0,0,2" />
                        <TextBox Text="{Binding ElementName=ActorsList, Path=SelectedItem.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                        <TextBlock Text="Alias (opcional)" Margin="0,0,0,2" />
                        <TextBox Text="{Binding ElementName=ActorsList, Path=SelectedItem.Alias, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                        <CheckBox Content="É ator (não participante)"
                                  IsChecked="{Binding ElementName=ActorsList, Path=SelectedItem.IsActor, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Mensagens -->
            <GroupBox Header="Mensagens">
                <StackPanel Margin="6">
                    <ListBox x:Name="MessagesList"
                             ItemsSource="{Binding Sequence.Messages}"
                             Height="100">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <Run Text="{Binding FromActor}" />
                                    <Run Text=" → " />
                                    <Run Text="{Binding ToActor}" />
                                    <Run Text=": " />
                                    <Run Text="{Binding Label}" FontStyle="Italic" />
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <Button Content="+ Adicionar" Click="OnAddMessage" Margin="0,0,6,0" />
                        <Button Content="Remover" Click="OnRemoveMessage" />
                    </StackPanel>

                    <StackPanel x:Name="MessageEditor" Margin="0,12,0,0"
                                IsEnabled="{Binding ElementName=MessagesList, Path=SelectedItem, Converter={StaticResource NullToFalseConverter}}">
                        <TextBlock Text="De" Margin="0,0,0,2" />
                        <ComboBox ItemsSource="{Binding Sequence.Actors}"
                                  DisplayMemberPath="Name" SelectedValuePath="Name"
                                  SelectedValue="{Binding ElementName=MessagesList, Path=SelectedItem.FromActor, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,0,0,6" />
                        <TextBlock Text="Para" Margin="0,0,0,2" />
                        <ComboBox ItemsSource="{Binding Sequence.Actors}"
                                  DisplayMemberPath="Name" SelectedValuePath="Name"
                                  SelectedValue="{Binding ElementName=MessagesList, Path=SelectedItem.ToActor, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,0,0,6" />
                        <TextBlock Text="Tipo" Margin="0,0,0,2" />
                        <ComboBox ItemsSource="{Binding Source={StaticResource SeqArrowsEnum}}"
                                  SelectedItem="{Binding ElementName=MessagesList, Path=SelectedItem.Arrow, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,0,0,6" />
                        <TextBlock Text="Label" Margin="0,0,0,2" />
                        <TextBox Text="{Binding ElementName=MessagesList, Path=SelectedItem.Label, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
```

- [ ] **Step 2: Criar code-behind**

`src/MeuMarkdown/Controls/MermaidBuilderPanels/SequenceBuilderPanel.xaml.cs`:
```csharp
using System.Windows;
using System.Windows.Controls;
using MeuMarkdown.Models.Mermaid;
using MeuMarkdown.ViewModels;

namespace MeuMarkdown.Controls.MermaidBuilderPanels;

public partial class SequenceBuilderPanel : UserControl
{
    public SequenceBuilderPanel()
    {
        InitializeComponent();
    }

    private MermaidBuilderViewModel? Vm => DataContext as MermaidBuilderViewModel;

    private void OnAddActor(object sender, RoutedEventArgs e)
    {
        if (Vm == null) return;
        var existingNames = Vm.Sequence.Actors.Select(a => a.Name).ToHashSet();
        var i = Vm.Sequence.Actors.Count + 1;
        while (existingNames.Contains($"Actor{i}")) i++;
        var actor = new Actor { Name = $"Actor{i}", IsActor = false };
        Vm.Sequence.Actors.Add(actor);
        ActorsList.SelectedItem = actor;
    }

    private void OnRemoveActor(object sender, RoutedEventArgs e)
    {
        if (Vm == null) return;
        if (ActorsList.SelectedItem is not Actor actor) return;
        Vm.Sequence.Actors.Remove(actor);
    }

    private void OnMoveActorUp(object sender, RoutedEventArgs e) => Move(ActorsList, Vm?.Sequence.Actors, -1);
    private void OnMoveActorDown(object sender, RoutedEventArgs e) => Move(ActorsList, Vm?.Sequence.Actors, +1);

    private void OnAddMessage(object sender, RoutedEventArgs e)
    {
        if (Vm == null) return;
        if (Vm.Sequence.Actors.Count < 1) return;
        var first = Vm.Sequence.Actors[0].Name;
        var second = Vm.Sequence.Actors.Count > 1 ? Vm.Sequence.Actors[1].Name : first;
        var msg = new Message { FromActor = first, ToActor = second, Arrow = SequenceArrowType.Sync, Label = "" };
        Vm.Sequence.Messages.Add(msg);
        MessagesList.SelectedItem = msg;
    }

    private void OnRemoveMessage(object sender, RoutedEventArgs e)
    {
        if (Vm == null) return;
        if (MessagesList.SelectedItem is not Message msg) return;
        Vm.Sequence.Messages.Remove(msg);
    }

    private static void Move<T>(ListBox list, System.Collections.ObjectModel.ObservableCollection<T>? coll, int delta)
    {
        if (coll == null) return;
        if (list.SelectedItem is not T item) return;
        var idx = coll.IndexOf(item);
        var newIdx = idx + delta;
        if (newIdx < 0 || newIdx >= coll.Count) return;
        coll.Move(idx, newIdx);
        list.SelectedItem = item;
    }
}
```

- [ ] **Step 3: Commit (build pode falhar — conversores ainda virão na Task 16)**

```powershell
git add src/MeuMarkdown/Controls/MermaidBuilderPanels/
git commit -m "feat(mermaid): SequenceBuilderPanel UserControl com formulário de atores e mensagens"
```

---

## Task 16 — `MermaidBuilderWindow` + conversores compartilhados

**Files:**
- Create: `src/MeuMarkdown/Views/MermaidBuilderWindow.xaml`
- Create: `src/MeuMarkdown/Views/MermaidBuilderWindow.xaml.cs`
- Create: `src/MeuMarkdown/Converters/EnumToBoolConverter.cs`
- Create: `src/MeuMarkdown/Converters/NullToFalseConverter.cs`
- Modify: `src/MeuMarkdown/App.xaml`

> Conversores DEVEM ser registrados em `App.xaml` (não em `<Window.Resources>`) — caso contrário não são visíveis dentro dos UserControls (FlowchartBuilderPanel, SequenceBuilderPanel), que foram instanciados em runtime via `new` no code-behind.

- [ ] **Step 1: Criar `EnumToBoolConverter`**

`src/MeuMarkdown/Converters/EnumToBoolConverter.cs`:
```csharp
using System.Globalization;
using System.Windows.Data;

namespace MeuMarkdown.Converters;

public class EnumToBoolConverter : IValueConverter
{
    public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
    {
        if (value == null || parameter == null) return false;
        return value.ToString() == parameter.ToString();
    }

    public object? ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
    {
        if (value is bool b && b && parameter != null && targetType.IsEnum)
            return Enum.Parse(targetType, parameter.ToString()!);
        return Binding.DoNothing;
    }
}
```

- [ ] **Step 2: Criar `NullToFalseConverter`**

`src/MeuMarkdown/Converters/NullToFalseConverter.cs`:
```csharp
using System.Globalization;
using System.Windows.Data;

namespace MeuMarkdown.Converters;

public class NullToFalseConverter : IValueConverter
{
    public object Convert(object value, Type targetType, object parameter, CultureInfo culture) => value != null;
    public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture) => Binding.DoNothing;
}
```

- [ ] **Step 2.5: Registrar conversores em `App.xaml`**

Em `src/MeuMarkdown/App.xaml`, dentro do `<ResourceDictionary>` (fora do `MergedDictionaries`), adicionar:

```xml
<conv:EnumToBoolConverter x:Key="EnumToBool" />
<conv:NullToFalseConverter x:Key="NullToFalseConverter" />
```

E declarar o namespace no topo do `<Application>`:
```xml
xmlns:conv="clr-namespace:MeuMarkdown.Converters"
```

O arquivo final fica:
```xml
<Application x:Class="MeuMarkdown.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MeuMarkdown"
             xmlns:conv="clr-namespace:MeuMarkdown.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Theme.Common.xaml" />
                <ResourceDictionary Source="/Themes/Icons.xaml" />
                <ResourceDictionary Source="/Themes/Theme.Dark.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <conv:EnumToBoolConverter x:Key="EnumToBool" />
            <conv:NullToFalseConverter x:Key="NullToFalseConverter" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
```

- [ ] **Step 3: Criar `MermaidBuilderWindow.xaml`**

```xml
<Window x:Class="MeuMarkdown.Views.MermaidBuilderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:MeuMarkdown.Converters"
        xmlns:panels="clr-namespace:MeuMarkdown.Controls.MermaidBuilderPanels"
        xmlns:controls="clr-namespace:MeuMarkdown.Controls"
        xmlns:vm="clr-namespace:MeuMarkdown.ViewModels"
        Title="Construtor visual de Mermaid"
        Width="1000" Height="650"
        MinWidth="780" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Bg.Window}"
        Foreground="{DynamicResource Brush.Fg.Primary}"
        FontFamily="Inter, Segoe UI Variable Text, Segoe UI"
        FontSize="13"
        Closing="OnClosing">
    <!-- Conversores EnumToBool e NullToFalseConverter vêm de App.xaml -->

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Topo: tipo -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Tipo:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox x:Name="TypeBox" Width="200" SelectionChanged="OnTypeChanged">
                <ComboBoxItem Content="Fluxograma" Tag="Flowchart" />
                <ComboBoxItem Content="Sequência" Tag="Sequence" />
            </ComboBox>
        </StackPanel>

        <!-- Split: formulário | preview -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderThickness="1" BorderBrush="{DynamicResource Brush.Border.Stronger}" CornerRadius="6">
                <ContentControl x:Name="PanelHost" />
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" Background="Transparent" />

            <Border Grid.Column="2" BorderThickness="1" BorderBrush="{DynamicResource Brush.Border.Stronger}" CornerRadius="6">
                <controls:MermaidPreviewLite x:Name="Preview" />
            </Border>
        </Grid>

        <!-- Expander de código -->
        <Expander Grid.Row="2" Header="Ver código Mermaid" Margin="0,10,0,0">
            <StackPanel>
                <TextBox Text="{Binding GeneratedCode, Mode=OneWay}"
                         IsReadOnly="True"
                         FontFamily="Cascadia Mono, Consolas"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         MinHeight="80" MaxHeight="160"
                         Margin="0,6,0,6" />
                <Button Content="Copiar" Click="OnCopyCode" HorizontalAlignment="Left" />
            </StackPanel>
        </Expander>

        <!-- Erro / aviso. Quando ErrorMessage é null, Text fica vazio e o bloco não ocupa espaço visível. -->
        <TextBlock Grid.Row="3"
                   Text="{Binding ErrorMessage}"
                   Foreground="#c0392b"
                   Margin="0,8,0,0"
                   TextWrapping="Wrap" />

        <!-- Botões -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="CancelButton" Content="Cancelar" Click="OnCancel" MinWidth="92" Margin="0,0,10,0" Padding="20,9" />
            <Button x:Name="OkButton" Content="Inserir" Click="OnOk" MinWidth="92" Padding="20,9" />
        </StackPanel>
    </Grid>
</Window>
```


- [ ] **Step 4: Criar `MermaidBuilderWindow.xaml.cs`**

```csharp
using System.Windows;
using System.Windows.Controls;
using MeuMarkdown.Controls;
using MeuMarkdown.Controls.MermaidBuilderPanels;
using MeuMarkdown.ViewModels;

namespace MeuMarkdown.Views;

public partial class MermaidBuilderWindow : Window
{
    private readonly MermaidBuilderViewModel _vm = new();
    public string? ResultMermaidCode { get; private set; }

    public MermaidBuilderWindow(string assetsDir, bool darkTheme)
    {
        InitializeComponent();
        DataContext = _vm;

        Preview.SetAssetsDirectory(assetsDir);
        Preview.SetDarkTheme(darkTheme);
        Preview.MermaidError += msg => Dispatcher.Invoke(() => _vm.SetRenderError(msg));

        _vm.RenderRequested += code => Dispatcher.Invoke(() => _ = Preview.RenderAsync(code));

        TypeBox.SelectedIndex = 0;
        SwapPanel(BuilderDiagramKind.Flowchart);
    }

    private void OnTypeChanged(object sender, SelectionChangedEventArgs e)
    {
        if (TypeBox.SelectedItem is not ComboBoxItem cbi) return;
        var tag = cbi.Tag as string;
        if (!Enum.TryParse<BuilderDiagramKind>(tag, out var kind)) return;
        if (kind == _vm.DiagramKind) return;

        if (_vm.IsModified)
        {
            var ok = MessageDialog.Confirm(this, "Trocar tipo",
                "Trocar de tipo descarta o que você fez. Continuar?",
                MessageDialogKind.Warning);
            if (!ok)
            {
                // Restaurar seleção
                TypeBox.SelectionChanged -= OnTypeChanged;
                TypeBox.SelectedIndex = _vm.DiagramKind == BuilderDiagramKind.Flowchart ? 0 : 1;
                TypeBox.SelectionChanged += OnTypeChanged;
                return;
            }
        }

        _vm.DiagramKind = kind;
        SwapPanel(kind);
    }

    private void SwapPanel(BuilderDiagramKind kind)
    {
        FrameworkElement panel = kind switch
        {
            BuilderDiagramKind.Flowchart => new FlowchartBuilderPanel(),
            BuilderDiagramKind.Sequence => new SequenceBuilderPanel(),
            _ => new FlowchartBuilderPanel(),
        };
        panel.DataContext = _vm;
        PanelHost.Content = panel;
    }

    private void OnOk(object sender, RoutedEventArgs e)
    {
        ResultMermaidCode = _vm.GeneratedCode;
        DialogResult = true;
        Close();
    }

    private void OnCancel(object sender, RoutedEventArgs e)
    {
        if (_vm.IsModified)
        {
            var ok = MessageDialog.Confirm(this, "Descartar diagrama",
                "Descartar o diagrama em edição?", MessageDialogKind.Question);
            if (!ok) return;
        }
        DialogResult = false;
        Close();
    }

    private void OnClosing(object? sender, System.ComponentModel.CancelEventArgs e)
    {
        if (DialogResult == true) return;
        if (_vm.IsModified && ResultMermaidCode == null)
        {
            var ok = MessageDialog.Confirm(this, "Descartar diagrama",
                "Descartar o diagrama em edição?", MessageDialogKind.Question);
            if (!ok) e.Cancel = true;
        }
    }

    private void OnCopyCode(object sender, RoutedEventArgs e)
    {
        try { Clipboard.SetText(_vm.GeneratedCode); }
        catch { /* ignora falha de clipboard */ }
    }
}
```

- [ ] **Step 5: Build — deve passar agora**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded.

- [ ] **Step 6: Commit**

```powershell
git add src/MeuMarkdown/Converters/ src/MeuMarkdown/Views/MermaidBuilderWindow.xaml src/MeuMarkdown/Views/MermaidBuilderWindow.xaml.cs
git commit -m "feat(mermaid): MermaidBuilderWindow + conversores compartilhados"
```

---

## Task 17 — Integrar construtor visual ao menu

**Files:**
- Modify: `src/MeuMarkdown/MainWindow.xaml.cs`

- [ ] **Step 1: Adicionar campo com path do assetsDir como membro**

Em `MainWindow.xaml.cs`, na seção onde o assetsDir é construído (linhas ~138-142), guardar em campo:

```csharp
private string _assetsDir = "";
```

E ajustar o trecho:
```csharp
_assetsDir = System.IO.Path.Combine(
    Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),
    "MeuMarkdown", "webview-assets");
_viewModel.MarkdownService.ExtractEnhancementAssetsTo(_assetsDir);
preview.RegisterEnhancementAssetsHost(_assetsDir);
```

- [ ] **Step 2: Substituir o handler `OnOpenMermaidBuilder` placeholder**

Trocar o método (que mostrava "Em breve") por:

```csharp
private void OnOpenMermaidBuilder(object sender, RoutedEventArgs e)
{
    var win = new Views.MermaidBuilderWindow(_assetsDir, _isDarkTheme)
    {
        Owner = this,
    };
    var ok = win.ShowDialog();
    if (ok == true && !string.IsNullOrWhiteSpace(win.ResultMermaidCode))
    {
        InsertMermaidBlock(win.ResultMermaidCode);
    }
}
```

- [ ] **Step 3: Build**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj
```
Expected: Build succeeded.

- [ ] **Step 4: Smoke manual**

```powershell
dotnet run --project src/MeuMarkdown/MeuMarkdown.csproj
```

No app:
- Abrir arquivo `.md`
- Menu Inserir > Mermaid > Construtor visual…
- Confirmar:
  - Janela abre com tipo "Fluxograma" e 2 nós + 1 aresta seed
  - Preview à direita renderiza o diagrama
  - Adicionar nó, editar label → preview atualiza em ~300ms
  - Trocar tipo pra "Sequência" → confirma descarte → painel troca pra atores/mensagens
  - Botão Cancelar com modificações → confirma descarte
  - Botão Inserir → janela fecha, bloco aparece no editor, preview principal renderiza
- Sem arquivo aberto: ainda assim deve abrir e inserir só quando houver tab (verificar comportamento — se quebrar, ajustar adicionando guard `if (textEditor == null) return;` que já existe em `InsertMermaidBlock`)

- [ ] **Step 5: Commit**

```powershell
git add src/MeuMarkdown/MainWindow.xaml.cs
git commit -m "feat(mermaid): integra construtor visual ao menu Inserir > Mermaid"
```

---

## Task 18 — Tema escuro do preview lite

**Files:**
- Modify: `src/MeuMarkdown/MainWindow.xaml.cs`
- Modify: `src/MeuMarkdown/Views/MermaidBuilderWindow.xaml.cs`

- [ ] **Step 1: Garantir que ToggleDarkTheme propaga para construtor aberto**

O construtor recebe `_isDarkTheme` no construtor mas não escuta mudanças. Solução pragmática v1: o tema escuro só é aplicado quando o construtor é aberto. Se o usuário trocar tema enquanto a janela estiver aberta, ela não acompanha. Isso está aceitável no v1 (documentado como limitação).

Nenhuma mudança de código aqui. Esta task documenta o comportamento e confirma via smoke:

- [ ] **Step 2: Smoke manual de tema**

```powershell
dotnet run --project src/MeuMarkdown/MeuMarkdown.csproj
```

- Trocar para tema escuro (F6)
- Abrir Inserir > Mermaid > Construtor visual…
- Confirmar: preview tem fundo escuro, diagrama renderiza com tema dark do Mermaid
- Cancelar, trocar pra tema claro, reabrir construtor
- Confirmar: preview com fundo claro, tema default do Mermaid

- [ ] **Step 3: Sem commit (sem mudanças de código)**

Pule essa task se o smoke passar.

---

## Task 19 — Atualizar `CHANGELOG.md`

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Adicionar entradas na seção `[Não lançado]`**

Abrir `CHANGELOG.md` e na seção `## [Não lançado]` (ou criar se não existir), adicionar:

```markdown
### Adicionado
- Menu **Inserir > Mermaid > Templates**: 8 esqueletos prontos comentados (Fluxograma, Sequência, Classe, ER, Estado, Gantt, Mindmap, Pie).
- **Construtor visual de Mermaid** (Inserir > Mermaid > Construtor visual…): janela com formulário e preview ao vivo para montar Fluxograma e Sequência sem escrever sintaxe. Suporta edição de nós/atores e arestas/mensagens, troca de tipo com confirmação, código gerado consultável e copiável.
```

- [ ] **Step 2: Commit**

```powershell
git add CHANGELOG.md
git commit -m "docs(changelog): entradas v1.10.0 (templates e construtor Mermaid)"
```

---

## Task 20 — Atualizar `README.md`

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Adicionar bullet em "Já entregue"**

Em `README.md`, dentro da seção "Já entregue" (a mais recente versão listada), adicionar uma linha referenciando a v1.10.0. Modelo (ajustar conforme o estilo já presente):

```markdown
- **v1.10.0** — Construtor visual de Mermaid: menu Inserir com 8 templates de diagrama + janela construtor com preview ao vivo para Fluxograma e Sequência.
```

- [ ] **Step 2: Commit**

```powershell
git add README.md
git commit -m "docs(readme): adiciona v1.10.0 à seção Já entregue"
```

---

## Task 21 — Smoke manual completo + verificação de testes

**Files:** nenhuma alteração — esta task valida o entregável.

- [ ] **Step 1: Rodar todos os testes**

```powershell
dotnet test tests/MeuMarkdown.Tests/MeuMarkdown.Tests.csproj
```
Expected: Passed, 0 Failed. Total de testes deve ter aumentado (templates + flowchart + sequence + insertion ≈ 30+ novos).

- [ ] **Step 2: Build em Release**

```powershell
dotnet build src/MeuMarkdown/MeuMarkdown.csproj -c Release
```
Expected: Build succeeded.

- [ ] **Step 3: Roteiro de smoke manual**

Rodar o app e verificar todos os itens abaixo:

```powershell
dotnet run --project src/MeuMarkdown/MeuMarkdown.csproj
```

- [ ] Cada um dos 8 templates insere bloco mermaid bem formado + preview renderiza
- [ ] Construtor visual: tipo Fluxograma carrega com seed (2 nós + 1 aresta), preview mostra diagrama
- [ ] Adicionar nó, editar label, mudar shape → preview atualiza em ~300ms
- [ ] Adicionar aresta entre nós existentes → preview mostra seta
- [ ] Aresta com FromId pra nó inexistente → não emite (sem quebra de render)
- [ ] Trocar pra Sequência com edição → diálogo de confirmação, descarte funciona
- [ ] Cancelar com modificações → diálogo de confirmação
- [ ] OK → janela fecha, bloco aparece no documento ativo, preview principal renderiza
- [ ] OK sem documento aberto → comportamento seguro (não deve crashar)
- [ ] Erro de sintaxe (caso provoque): mensagem aparece em vermelho, OK ainda funciona
- [ ] Tema escuro: abrir construtor com F6 ativo → preview com fundo escuro

Se algum item falhar, retornar à task correspondente, corrigir, recommit, repetir.

- [ ] **Step 4: Sem commit (apenas validação)**

---

## Task 22 — Release v1.10.0

Esta task usa a skill `/release` deste repo (`.claude/skills/release/`). Os passos abaixo refletem o que a skill faz; pode executá-los manualmente ou delegar à skill.

- [ ] **Step 1: Confirmar working tree limpa**

```powershell
git status
```
Expected: `nothing to commit, working tree clean` (com a branch feat/v1.10.0).

- [ ] **Step 2: Mover entradas do CHANGELOG**

Editar `CHANGELOG.md`: mover bloco de `[Não lançado]` para `## [1.10.0] — 2026-05-21`, criar `[Não lançado]` vazia acima, atualizar links no rodapé do arquivo.

- [ ] **Step 3: Merge na main**

```powershell
git checkout main
git pull --ff-only
git merge --no-ff feat/v1.10.0 -m "Merge feat/v1.10.0 — Construtor visual de Mermaid"
```

- [ ] **Step 4: Commit do release + tag**

```powershell
git add CHANGELOG.md
git commit -m "chore: release v1.10.0"
git tag v1.10.0
git push origin main v1.10.0
```

- [ ] **Step 5: Verificar GitHub Actions**

Após o push da tag, o workflow `.github/workflows/release.yml` dispara. Acompanhar em https://github.com/<owner>/meu-markdown/actions. Expected: build verde, instalador gerado, GitHub Release criado com release notes extraídas do CHANGELOG.

- [ ] **Step 6: Smoke final no instalador (opcional, mas recomendado)**

Após release publicado, baixar `MeuMarkdown-Setup-v1.10.0.exe`, instalar em máquina limpa, abrir, repetir 3 itens-chave do smoke (template, construtor, render no preview principal).

---

## Self-Review (executar após escrever o plano — antes de oferecer execução)

**1. Spec coverage:**

| Spec | Task que implementa |
|---|---|
| 8 templates por tipo | Task 1, Task 2 |
| Menu Inserir > Mermaid + 8 itens + construtor | Task 10, Task 17 |
| Inserção no cursor com newlines | Task 9, Task 10 |
| MermaidBuilderViewModel + debounce 300ms | Task 13 |
| FlowchartBuilderPanel | Task 14 |
| SequenceBuilderPanel | Task 15 |
| MermaidBuilderWindow + split + expander + botões + tipo | Task 16, Task 17 |
| MermaidPreviewLite + virtual host | Task 11, Task 12 |
| Tratamento de erro Mermaid | Task 12, Task 16 |
| Trocar tipo com confirmação | Task 16 |
| Cancelar com confirmação | Task 16 |
| ToMermaid + escape + validação Flowchart | Task 4, Task 5, Task 6 |
| ToMermaid + escape Sequence | Task 8 |
| Estado inicial seed | Task 13 |
| Tema escuro no preview | Task 12 (suporte), Task 18 (verificação) |
| Testes unitários | Task 2, Task 4-6, Task 8, Task 9 |
| Smoke manual documentado | Task 10, Task 17, Task 21 |
| CHANGELOG/README | Task 19, Task 20 |
| Release v1.10.0 | Task 22 |

Cobertura completa.

**2. Placeholder scan:** revisado. Sem TBD/TODO. Todo código está completo nos steps.

**3. Type consistency:** nomes verificados — `MermaidDiagramType`, `BuilderDiagramKind`, `FlowchartModel`, `SequenceModel`, `MermaidBuilderViewModel`, `MermaidBuilderWindow`, `MermaidPreviewLite`, `InsertMermaidBlock`, `BuildMermaidInsertion`, `MermaidTemplateService`, `MarkdownInsertionService` — todos coerentes entre tasks.
