<UserControl x:Class="MeuMarkdown.Controls.ActivityBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:MeuMarkdown.Controls"
             Width="48"
             Background="{DynamicResource Brush.Bg.SurfaceAlt}">
    <UserControl.Resources>
        <Style x:Key="ActivityBtn" TargetType="RadioButton">
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="0,3" />
            <Setter Property="GroupName" Value="ActivityBar" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Fg.Tertiary}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="Transparent" CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{DynamicResource Brush.Bg.Hover}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource Brush.Fg.Secondary}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{DynamicResource Brush.Bg.Active}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource Brush.Fg.Primary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <DockPanel HorizontalAlignment="Center">
        <!-- Settings fica no rodapé, separado dos outros -->
        <StackPanel DockPanel.Dock="Bottom" Margin="0,0,0,6">
            <RadioButton x:Name="SettingsBtn" Style="{StaticResource ActivityBtn}"
                         ToolTip="Configurações"
                         Checked="OnPanelChecked" Tag="Settings">
                <controls:Icon Geometry="{StaticResource Icon.Settings}" Size="18" />
            </RadioButton>
        </StackPanel>

        <StackPanel DockPanel.Dock="Top" Margin="0,6,0,0">
            <RadioButton x:Name="ExplorerBtn" Style="{StaticResource ActivityBtn}"
                         ToolTip="Explorer"
                         Checked="OnPanelChecked" Tag="Explorer">
                <controls:Icon Geometry="{StaticResource Icon.Folder}" Size="18" />
            </RadioButton>
            <RadioButton x:Name="OutlineBtn" Style="{StaticResource ActivityBtn}"
                         ToolTip="Outline (Ctrl+Shift+O)"
                         Checked="OnPanelChecked" Tag="Outline">
                <controls:Icon Geometry="{StaticResource Icon.ListTree}" Size="18" />
            </RadioButton>
            <RadioButton x:Name="SearchBtn" Style="{StaticResource ActivityBtn}"
                         ToolTip="Buscar"
                         Checked="OnPanelChecked" Tag="Search">
                <controls:Icon Geometry="{StaticResource Icon.Search}" Size="18" />
            </RadioButton>
        </StackPanel>
    </DockPanel>
</UserControl>
